\documentclass[tikz,border=8pt]{standalone}
\usepackage[T1]{fontenc}
\usepackage{lmodern}
\input{landingpages-figure-system.tex}
\begin{document}
\begin{tikzpicture}[mqtt mobile]

\node[mqtt section title] (title) {One concrete endpoint};
\node[mqtt config,below=\MQGapS of title,minimum width=74mm,text width=64mm] (address)
  {\textbf{Address family}\\IPv4 · IPv6 · Unix domain\\RFCOMM · L2CAP};
\node[mqtt evidence source,below=\MQGapXS of address.south east,anchor=north east] (bluetooth) {RFCOMM / L2CAP\\source-verified};
\node[mqtt config,below={\MQGapXL+\MQGapXS} of address,minimum width=74mm,text width=64mm] (role)
  {\textbf{Endpoint role}\\\texttt{SocketServer} · \texttt{SocketClient}};
\node[mqtt config,below=\MQGapM of role,minimum width=74mm,text width=64mm] (mode)
  {\textbf{Connection mode}\\plain stream · OpenSSL-backed TLS};
\node[mqtt config,below=\MQGapM of mode,minimum width=74mm,text width=64mm] (context)
  {\textbf{Protocol / application context}\\custom \texttt{SocketContext} or compatible protocol context};

\draw[mqtt association] (address.south) -- (role.north);
\path (address.east) ++(\MQGapXS,0) coordinate (evidencerail);
\draw[mqtt observation] (address.east) -- (evidencerail) |- (bluetooth.east);
\draw[mqtt association] (role.south) -- (mode.north);
\draw[mqtt association] (mode.south) -- (context.north);

\node[mqtt control,below={\MQMobileLaneGap+\MQGroupPad} of context,minimum width=74mm,text width=64mm] (runtime)
  {\textbf{Shared event runtime}\\selected multiplexer drives endpoint and connection work};
\node[mqtt note,below=\MQGapM of runtime,minimum width=76mm,text width=66mm] (qualification)
  {Compatible typed compositions only; not every theoretical cross-product is built, tested, or runtime-qualified.};
\path (title.north) ++(0,\MQGroupPad) coordinate (outertop);
\path (evidencerail) ++(\MQGroupPad,0) coordinate (outerright);

\begin{scope}[on background layer]
  \node[mqtt figure frame,fit=(outertop)(outerright)(title)(address)(bluetooth)(evidencerail)(role)(mode)(context)(runtime)(qualification)] {};
  \node[mqtt group,fit=(title)(address)(bluetooth)(evidencerail)(role)(mode)(context)] (endpoint) {};
\end{scope}

\draw[mqtt association] (endpoint.south) -- node[mqtt edge label,right] {uses} (runtime.north);

\end{tikzpicture}
\end{document}
