\documentclass[tikz,border=8pt]{standalone}
\usepackage[T1]{fontenc}
\usepackage{lmodern}
\input{landingpages-figure-system.tex}
\begin{document}
\begin{tikzpicture}[mqtt desktop]

\node[mqtt section title] (title) {One concrete endpoint};
\node[mqtt evidence source,right=\MQGapS of title] (bluetooth)
  {\shortstack{RFCOMM / L2CAP\\source-verified}};
\path (title.south) -- (bluetooth.south) coordinate[midway] (headerbottom);
\node[mqtt config,below=\MQGapS of headerbottom,minimum width=92mm,text width=82mm] (address)
  {\textbf{Address family}\\IPv4 · IPv6 · Unix domain · RFCOMM · L2CAP};
\node[mqtt config,below=\MQGapM of address,minimum width=92mm,text width=82mm] (role)
  {\textbf{Endpoint role}\\\texttt{SocketServer} · \texttt{SocketClient}};
\node[mqtt config,below=\MQGapM of role,minimum width=92mm,text width=82mm] (mode)
  {\textbf{Connection mode}\\plain stream · OpenSSL-backed TLS};
\node[mqtt config,below=\MQGapM of mode,minimum width=92mm,text width=82mm] (context)
  {\textbf{Protocol / application context}\\custom \texttt{SocketContext} or compatible framework protocol context};

\draw[mqtt association] (address.south) -- (role.north);
\draw[mqtt association] (role.south) -- (mode.north);
\draw[mqtt association] (mode.south) -- (context.north);
\path (bluetooth.east) ++(\MQGapXS,0) coordinate (evidencerail);
\path (evidencerail |- address.east) coordinate (evidencefrom);
\draw[mqtt observation] (address.east) -- (evidencefrom) -- (evidencerail) -- (bluetooth.east);

\node[mqtt control,below={\MQGapM+\MQGroupPad} of context,minimum width=92mm,text width=82mm] (runtime)
  {\textbf{Shared event runtime}\\one selected multiplexer drives endpoint and connection work};
\node[mqtt note,below=\MQGapM of runtime,minimum width=96mm,text width=86mm] (qualification)
  {Compatible typed compositions only. Component presence does not imply that every cross-product is built, tested, or runtime-qualified.};
\path (title.north) ++(0,\MQGroupPad) coordinate (outertop);
\path (evidencerail) ++(\MQGroupPad,0) coordinate (outerright);

\begin{scope}[on background layer]
  \node[mqtt figure frame,fit=(outertop)(outerright)(title)(bluetooth)(address)(evidencerail)(role)(mode)(context)(runtime)(qualification)] {};
  \node[mqtt group,fit=(title)(bluetooth)(address)(evidencerail)(role)(mode)(context)] (endpoint) {};
\end{scope}

\path (endpoint.south -| context.center) coordinate (endpoint-south-axis);
\draw[mqtt association] (endpoint-south-axis) -- node[mqtt edge label,right] {uses} (runtime.north);

\end{tikzpicture}
\end{document}
