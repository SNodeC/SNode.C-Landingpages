\documentclass[tikz,border=8pt]{standalone}
\usepackage[T1]{fontenc}
\usepackage{lmodern}
\input{landingpages-figure-system.tex}
\begin{document}
\begin{tikzpicture}[mqtt mobile]

\node[mqtt config,mqtt node compact] (instancea) {named endpoint\\instance A};
\node[mqtt config,right=\MQHorizontalGapTight of instancea,mqtt node compact] (instanceb)
  {named endpoint\\instance B};
\path (instancea.north) -- (instanceb.north) coordinate[midway] (instancesmid);
\node[mqtt application,above=\MQConnectorSpan of instancesmid,mqtt node extra wide] (app)
  {application-owned \texttt{SubCommand}};
\node[mqtt section title,above=\MQGapS of app] (namedtitle) {Named root children};
\node[mqtt config,above=\MQSectionSeparation of namedtitle,mqtt node extra wide] (root)
  {\texttt{ConfigRoot}\\application configuration root};

\node[mqtt config,below=\MQSectionSeparation of instancea,mqtt node compact] (local) {\texttt{local}};
\node[mqtt config,below=\MQSectionSeparation of instanceb,mqtt node compact] (remote) {\texttt{remote}};
\path (local.south) -- (remote.south) coordinate[midway] (firstbottom);
\node[mqtt section title,above=\MQGapS of firstbottom] (sectionstitle) {Instance A sections};
\node[mqtt config,below=\MQGapS of firstbottom,mqtt node extra wide] (connection) {\texttt{connection}};
\node[mqtt config,below=\MQSectionSeparation of local,mqtt node compact] (socket) {\texttt{socket}};
\node[mqtt config,below=\MQSectionSeparation of remote,mqtt node compact] (tls)
  {\texttt{tls}\\when selected};

\path (socket.south) -- (tls.south) coordinate[midway] (sectionsbottom);
\node[mqtt note,below=\MQGapXL of sectionsbottom,mqtt node extra wide] (dependent)
  {Section availability is role/family/mode dependent;\\the figure shows the reusable\\section vocabulary.};
\node[mqtt note,below=\MQGapM of dependent,mqtt node extra wide] (anonymous)
  {Anonymous endpoint: API-configurable only;\\it is not a named \texttt{ConfigRoot} child\\and has no named CLI/config-file\\address.};

\begin{scope}[on background layer]
  \node[mqtt figure frame,fit=(root)(namedtitle)(app)(instancea)(instanceb)(sectionstitle)(local)(remote)(connection)(socket)(tls)(dependent)(anonymous)] {};
  \node[mqtt group,fit=(namedtitle)(app)(instancea)(instanceb)] (children) {};
  \node[mqtt group,fit=(sectionstitle)(local)(remote)(connection)(socket)(tls)] (sections) {};
\end{scope}

\draw[mqtt contains] (root.south) -- node[mqtt edge label right] {contains} (children.north);
\path (sections.north -| instancea.center) coordinate (sections-instancea-north);
\draw[mqtt contains] (instancea.south) -- node[mqtt edge label right] {assembles} (sections-instancea-north);

\end{tikzpicture}
\end{document}
