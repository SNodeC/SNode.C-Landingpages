\documentclass[tikz,border=8pt]{standalone}
\usepackage[T1]{fontenc}
\usepackage{lmodern}
\input{landingpages-figure-system.tex}
\begin{document}
\begin{tikzpicture}[mqtt mobile]

\node[mqtt section title] (title) {Same established \texttt{SocketConnection}};
\path (title.south) ++(0,-\MQGapM) coordinate (toprow);
\node[mqtt state,left=\MQGapM of toprow,anchor=east,minimum width=30mm,text width=24mm] (http)
  {HTTP context\\ACTIVE};
\node[mqtt process,right=\MQGapM of toprow,anchor=west,minimum width=36mm,text width=30mm] (staged)
  {WebSocket replacement\\STAGED, not active};
\path (http.east) -- (staged.west) coordinate[midway] (handoffmid);
\draw[mqtt handoff] (http.east) -- (staged.west);
\node[mqtt annotation,above=\MQGapS of handoffmid] (handofflabel) {Upgrade accepted};

\path (http.south) -- (staged.south) coordinate[midway] (topmid);
\node[mqtt process,below=\MQGapM of topmid,minimum width=56mm,text width=48mm] (return)
  {current HTTP read callback returns};
\path (http.south) -- (return.north) coordinate[midway] (returnmid);
\draw[mqtt control flow] (http.south) -- (http.south |- returnmid) -- (return.north |- returnmid) -- (return.north);

\node[mqtt control,below=\MQGapM of return,minimum width=60mm,text width=52mm] (switch)
  {\texttt{detach(ContextSwitch)}\\select staged context\\\texttt{attach()}};
\node[mqtt state,below=\MQGapM of switch,minimum width=52mm] (websocket) {WebSocket context --- ACTIVE};
\draw[mqtt control flow] (return.south) -- (switch.north);
\path (staged.east) -- (switch.east) coordinate[midway] (dependencycenter);
\draw[mqtt dependency] (staged.east) -- (dependencycenter |- staged.east) -- (dependencycenter |- switch.east) -- (switch.east);
\draw[mqtt control flow] (switch.south) -- (websocket.north);

\node[mqtt note,below=\MQGapM of websocket,minimum width=70mm,text width=60mm] (note)
  {HTTP stays active until the callback returns. The same transport connection continues. SSE/EventSource does not replace the context.};

\begin{scope}[on background layer]
  \node[mqtt figure frame,fit=(title)(http)(staged)(handofflabel)(return)(switch)(dependencycenter)(websocket)(note)] {};
\end{scope}

\end{tikzpicture}
\end{document}
