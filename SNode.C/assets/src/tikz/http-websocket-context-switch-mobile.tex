\documentclass[tikz,border=8pt]{standalone}
\usepackage[T1]{fontenc}
\usepackage{lmodern}
\input{landingpages-figure-system.tex}
\begin{document}
\begin{tikzpicture}[mqtt mobile]

\node[mqtt section title] (title) {Same established\\\texttt{SocketConnection}};
\node[mqtt state,below=\MQGapM of title,minimum width=30mm,minimum height=22mm,text width=24mm] (http)
  {HTTP context\\ACTIVE};
\node[mqtt process,right=\MQGapM of http,minimum width=32mm,minimum height=22mm,text width=26mm] (staged)
  {WebSocket replacement\\STAGED after Upgrade\\not active};
\draw[mqtt handoff] (http.east) -- (staged.west);

\node[mqtt process,below=\MQGapM of http,minimum width=56mm,text width=48mm] (return)
  {current HTTP read callback returns};
\node[mqtt control,below=\MQGapM of return,minimum width=58mm,text width=50mm] (switch)
  {\texttt{detach(ContextSwitch)}\\select staged context\\\texttt{attach()}};
\node[mqtt state,below=\MQGapM of switch,minimum width=52mm] (websocket)
  {WebSocket context\\ACTIVE};

\draw[mqtt control flow] (http.south) -- (return.north);
\draw[mqtt control flow] (return.south) -- (switch.north);
\draw[mqtt dependency] (staged.south) |- (switch.east);
\draw[mqtt control flow] (switch.south) -- (websocket.north);

\node[mqtt note,below=\MQGapM of websocket,minimum width=58mm,text width=50mm] (note)
  {HTTP stays active until the callback returns. The same transport connection continues. SSE/EventSource does not replace the context.};

\begin{scope}[on background layer]
  \node[mqtt figure frame,fit=(title)(http)(staged)(return)(switch)(websocket)(note)] {};
\end{scope}

\end{tikzpicture}
\end{document}
