\documentclass[tikz,border=8pt]{standalone}
\usepackage[T1]{fontenc}
\usepackage{lmodern}
\input{landingpages-figure-system.tex}
\begin{document}
\begin{tikzpicture}[mqtt desktop]

\node[mqtt process,minimum width=44mm] (server) {\texttt{SocketServer}\\server endpoint};
\node[mqtt section title,above=\MQGapXS of server] (servertitle) {Server flow};
\node[mqtt state,below=\MQGapM of server,minimum width=44mm] (serverconn) {\texttt{SocketConnection}\\established stream};
\node[mqtt process,below=\MQGapM of serverconn,minimum width=48mm] (serverfactory) {\texttt{SocketContextFactory}\\retained by endpoint flow};
\node[mqtt application,below=\MQGapM of serverfactory,minimum width=48mm] (serverctx) {\texttt{SocketContext}\\one active per connection};

\draw[mqtt flow] (server.south) -- node[mqtt edge label,right] {listen $\rightarrow$ accept} (serverconn.north);
\draw[mqtt flow] (serverconn.south) -- node[mqtt edge label,right] {\texttt{create(this)}} (serverfactory.north);
\draw[mqtt flow] (serverfactory.south) -- node[mqtt edge label,right] {returns new} (serverctx.north);
\draw[mqtt association] (serverconn.west) -- ++(-\MQGapS,0) |- (serverctx.west);

\begin{scope}[on background layer]
  \node[mqtt figure frame,fit=(servertitle)(server)(serverconn)(serverfactory)(serverctx)] (serverframe) {};
\end{scope}

\node[mqtt process,right=\MQGapXL of server,minimum width=44mm] (client) {\texttt{SocketClient}\\client endpoint};
\node[mqtt section title,above=\MQGapXS of client] (clienttitle) {Client flow};
\node[mqtt state,below=\MQGapM of client,minimum width=44mm] (clientconn) {\texttt{SocketConnection}\\established stream};
\node[mqtt process,below=\MQGapM of clientconn,minimum width=48mm] (clientfactory) {\texttt{SocketContextFactory}\\retained by endpoint flow};
\node[mqtt application,below=\MQGapM of clientfactory,minimum width=48mm] (clientctx) {\texttt{SocketContext}\\one active per connection};

\draw[mqtt flow] (client.south) -- node[mqtt edge label,right] {connect completes} (clientconn.north);
\draw[mqtt flow] (clientconn.south) -- node[mqtt edge label,right] {\texttt{create(this)}} (clientfactory.north);
\draw[mqtt flow] (clientfactory.south) -- node[mqtt edge label,right] {returns new} (clientctx.north);
\draw[mqtt association] (clientconn.east) -- ++(\MQGapS,0) |- (clientctx.east);

\begin{scope}[on background layer]
  \node[mqtt figure frame,fit=(clienttitle)(client)(clientconn)(clientfactory)(clientctx)] (clientframe) {};
\end{scope}

\path (serverframe.south) -- (clientframe.south) coordinate[midway] (lanemid);
\node[mqtt control,below=\MQGapL of lanemid,minimum width=132mm,text width=122mm] (loop)
  {\textbf{Event loop --- \texttt{start()}}\\descriptor readiness, timers, lifecycle and data callbacks run on the caller thread};

\end{tikzpicture}
\end{document}
