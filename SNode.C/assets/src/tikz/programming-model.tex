\documentclass[tikz,border=8pt]{standalone}
\usepackage[T1]{fontenc}
\usepackage{lmodern}
\input{landingpages-figure-system.tex}
\begin{document}
\begin{tikzpicture}[mqtt desktop]

\node[mqtt process,minimum width=48mm] (server) {\texttt{SocketServer}\\server endpoint};
\node[mqtt section title,above=\MQGapXS of server] (servertitle) {Server flow};
\node[mqtt state,below=\MQGapM of server,minimum width=48mm] (serverconn) {\texttt{SocketConnection}\\established stream};
\node[mqtt process,below=\MQGapM of serverconn,minimum width=48mm] (serverfactory) {\texttt{SocketContextFactory}\\retained by endpoint flow};
\node[mqtt application,below=\MQGapM of serverfactory,minimum width=48mm] (serverctx) {\texttt{SocketContext}\\one active per connection};
\path (serverconn.west) ++(-\MQGapS,0) coordinate (serverrail);

\draw[mqtt flow] (server.south) -- node[mqtt edge label right] {listen $\rightarrow$ accept} (serverconn.north);
\draw[mqtt flow] (serverconn.south) -- node[mqtt edge label right] {\texttt{create(this)}} (serverfactory.north);
\draw[mqtt flow] (serverfactory.south) -- node[mqtt edge label right] {returns new} (serverctx.north);
\draw[mqtt association] (serverconn.west) -- (serverrail) |- (serverctx.west);

\node[mqtt process,right={\MQLaneGap+2*\MQGroupPad} of server,minimum width=48mm] (client) {\texttt{SocketClient}\\client endpoint};
\node[mqtt section title,above=\MQGapXS of client] (clienttitle) {Client flow};
\node[mqtt state,below=\MQGapM of client,minimum width=48mm] (clientconn) {\texttt{SocketConnection}\\established stream};
\node[mqtt process,below=\MQGapM of clientconn,minimum width=48mm] (clientfactory) {\texttt{SocketContextFactory}\\retained by endpoint flow};
\node[mqtt application,below=\MQGapM of clientfactory,minimum width=48mm] (clientctx) {\texttt{SocketContext}\\one active per connection};
\path (clientconn.east) ++(\MQGapS,0) coordinate (clientrail);

\draw[mqtt flow] (client.south) -- node[mqtt edge label left] {connect completes} (clientconn.north);
\draw[mqtt flow] (clientconn.south) -- node[mqtt edge label left] {\texttt{create(this)}} (clientfactory.north);
\draw[mqtt flow] (clientfactory.south) -- node[mqtt edge label left] {returns new} (clientctx.north);
\draw[mqtt association] (clientconn.east) -- (clientrail) |- (clientctx.east);

\path (serverctx.south) -- (clientctx.south) coordinate[midway] (lanemid);
\node[mqtt control,below={\MQGapM+\MQGroupPad} of lanemid,minimum width=106mm,text width=96mm] (loop)
  {\textbf{Event loop --- \texttt{start()}}\\descriptor readiness, timers, lifecycle and data callbacks run on the caller thread};

\path (servertitle.north) ++(0,\MQGroupPad) coordinate (outertop);
\path (serverrail) ++(-\MQGroupPad,0) coordinate (outerleft);
\path (clientrail) ++(\MQGroupPad,0) coordinate (outerright);

\begin{scope}[on background layer]
  \node[mqtt figure frame,fit=(outertop)(outerleft)(outerright)(servertitle)(server)(serverconn)(serverfactory)(serverctx)(serverrail)(clienttitle)(client)(clientconn)(clientfactory)(clientctx)(clientrail)(loop)] {};
  \node[mqtt group,fit=(servertitle)(server)(serverconn)(serverfactory)(serverctx)(serverrail)] (servergroup) {};
  \node[mqtt group,fit=(clienttitle)(client)(clientconn)(clientfactory)(clientctx)(clientrail)] (clientgroup) {};
\end{scope}

\end{tikzpicture}
\end{document}
