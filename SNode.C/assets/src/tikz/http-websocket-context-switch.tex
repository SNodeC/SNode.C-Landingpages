\documentclass[tikz,border=8pt]{standalone}
\usepackage[T1]{fontenc}
\usepackage{lmodern}
\input{landingpages-figure-system.tex}
\begin{document}
\begin{tikzpicture}[mqtt desktop]

\node[mqtt section title] (title) {Same established \texttt{SocketConnection}};
\node[mqtt state,below=\MQConnectorSpan of title,mqtt node wide] (http)
  {HTTP context\\ACTIVE};
\node[mqtt process,right=\MQConnectorSpan of http,mqtt node wide] (staged)
  {WebSocket replacement\\STAGED after Upgrade\\not active};
\draw[mqtt handoff] (http.east) -- (staged.west);

\node[mqtt process,below=\MQConnectorSpan of http,mqtt node wide] (return)
  {current HTTP read\\callback returns};
\node[mqtt control,below=\MQConnectorSpan of return,mqtt node wide] (switch)
  {\texttt{detach(ContextSwitch)}\\select staged context\\\texttt{attach()}};
\node[mqtt state,below=\MQConnectorSpan of switch,mqtt node wide] (websocket)
  {WebSocket context\\ACTIVE};

\draw[mqtt control flow] (http.south) -- (return.north);
\draw[mqtt control flow] (return.south) -- (switch.north);
\draw[mqtt dependency] (staged.south) |- (switch.east);
\draw[mqtt control flow] (switch.south) -- (websocket.north);

\node[mqtt note,below=\MQConnectorSpan of websocket,mqtt node hero] (note)
  {HTTP remains active until its current read callback returns.\\The same connection and transport continue through the switch.\\SSE/EventSource does not use this replacement path.};

\begin{scope}[on background layer]
  \node[mqtt figure frame,fit=(title)(http)(staged)(return)(switch)(websocket)(note)] {};
\end{scope}

\end{tikzpicture}
\end{document}
