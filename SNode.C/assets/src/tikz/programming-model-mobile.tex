\documentclass[tikz,border=8pt]{standalone}
\usepackage[T1]{fontenc}
\usepackage{lmodern}
\input{landingpages-figure-system.tex}
\begin{document}
\begin{tikzpicture}[mqtt mobile]

\node[mqtt section title] (servertitle) {Server flow};
\node[mqtt process,below=\MQGapS of servertitle,mqtt node wide] (server)
  {\texttt{SocketServer} server endpoint};
\node[mqtt state,below=\MQConnectorSpan of server,mqtt node wide] (serverconn)
  {\texttt{SocketConnection} established stream};
\node[mqtt process,below=\MQConnectorSpan of serverconn,mqtt node wide] (serverfactory)
  {\texttt{SocketContextFactory}\\retained by endpoint flow};
\node[mqtt state,below=\MQConnectorSpan of serverfactory,mqtt node wide] (serverctx)
  {\texttt{SocketContext}\\one active per connection};

\draw[mqtt flow] (server.south) -- node[mqtt edge label right] {listen then accept} (serverconn.north);
\draw[mqtt flow] (serverconn.south) -- node[mqtt edge label right] {\texttt{create(this)}} (serverfactory.north);
\draw[mqtt flow] (serverfactory.south) -- node[mqtt edge label right] {returns new} (serverctx.north);

\node[mqtt group,fit=(servertitle)(server)(serverconn)(serverfactory)(serverctx)] (servergroup) {};

\node[mqtt control,below=\MQRhythmCompact of servergroup,mqtt node extra wide] (loop)
  {\textbf{Shared event loop --- \texttt{start()}}\\both endpoint flows dispatch callbacks\\on the caller thread};

\node[mqtt section title,below=\MQGapM of loop] (clienttitle) {Client flow};
\node[mqtt process,below=\MQGapS of clienttitle,mqtt node wide] (client)
  {\texttt{SocketClient} client endpoint};
\node[mqtt state,below=\MQConnectorSpan of client,mqtt node wide] (clientconn)
  {\texttt{SocketConnection} established stream};
\node[mqtt process,below=\MQConnectorSpan of clientconn,mqtt node wide] (clientfactory)
  {\texttt{SocketContextFactory}\\retained by endpoint flow};
\node[mqtt state,below=\MQConnectorSpan of clientfactory,mqtt node wide] (clientctx)
  {\texttt{SocketContext}\\one active per connection};

\draw[mqtt flow] (client.south) -- node[mqtt edge label right] {connect completes} (clientconn.north);
\draw[mqtt flow] (clientconn.south) -- node[mqtt edge label right] {\texttt{create(this)}} (clientfactory.north);
\draw[mqtt flow] (clientfactory.south) -- node[mqtt edge label right] {returns new} (clientctx.north);

\node[mqtt group,fit=(clienttitle)(client)(clientconn)(clientfactory)(clientctx)] (clientgroup) {};
\begin{scope}[on background layer]
  \node[mqtt figure frame,fit=(servergroup)(loop)(clientgroup)] {};
\end{scope}

\draw[mqtt association] (servergroup.south) -- (loop.north);
\draw[mqtt association] (loop.south) -- (clientgroup.north);

\end{tikzpicture}
\end{document}
