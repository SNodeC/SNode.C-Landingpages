\documentclass[tikz,border=8pt]{standalone}
\usepackage[T1]{fontenc}
\usepackage{lmodern}
\input{landingpages-figure-system.tex}
\begin{document}
\begin{tikzpicture}[mqtt mobile]

\node[mqtt section title] (servertitle) {Server flow};
\node[mqtt process,below=\MQGapXS of servertitle,minimum width=52mm] (server) {\texttt{SocketServer}\\server endpoint};
\node[mqtt state,below=\MQGapM of server,minimum width=52mm] (serverconn) {\texttt{SocketConnection}\\established stream};
\node[mqtt process,below=\MQGapM of serverconn,minimum width=62mm] (serverfactory) {\texttt{SocketContextFactory}\\retained by endpoint flow};
\node[mqtt application,below=\MQGapM of serverfactory,minimum width=62mm] (serverctx) {\texttt{SocketContext}\\one active per connection};

\draw[mqtt flow] (server.south) -- node[mqtt edge label,right] {listen $\rightarrow$ accept} (serverconn.north);
\draw[mqtt flow] (serverconn.south) -- node[mqtt edge label,right] {\texttt{create(this)}} (serverfactory.north);
\draw[mqtt flow] (serverfactory.south) -- node[mqtt edge label,right] {returns new} (serverctx.north);
\draw[mqtt association] (serverconn.east) -- ++(\MQGapS,0) |- (serverctx.east);

\begin{scope}[on background layer]
  \node[mqtt figure frame,fit=(servertitle)(server)(serverconn)(serverfactory)(serverctx)] (serverframe) {};
\end{scope}

\node[mqtt section title,below=\MQGapXL of serverframe] (clienttitle) {Client flow};
\node[mqtt process,below=\MQGapXS of clienttitle,minimum width=52mm] (client) {\texttt{SocketClient}\\client endpoint};
\node[mqtt state,below=\MQGapM of client,minimum width=52mm] (clientconn) {\texttt{SocketConnection}\\established stream};
\node[mqtt process,below=\MQGapM of clientconn,minimum width=62mm] (clientfactory) {\texttt{SocketContextFactory}\\retained by endpoint flow};
\node[mqtt application,below=\MQGapM of clientfactory,minimum width=62mm] (clientctx) {\texttt{SocketContext}\\one active per connection};

\draw[mqtt flow] (client.south) -- node[mqtt edge label,right] {connect completes} (clientconn.north);
\draw[mqtt flow] (clientconn.south) -- node[mqtt edge label,right] {\texttt{create(this)}} (clientfactory.north);
\draw[mqtt flow] (clientfactory.south) -- node[mqtt edge label,right] {returns new} (clientctx.north);
\draw[mqtt association] (clientconn.east) -- ++(\MQGapS,0) |- (clientctx.east);

\begin{scope}[on background layer]
  \node[mqtt figure frame,fit=(clienttitle)(client)(clientconn)(clientfactory)(clientctx)] (clientframe) {};
\end{scope}

\node[mqtt control,below=\MQGapXL of clientframe,minimum width=84mm,text width=74mm] (loop)
  {\textbf{Event loop --- \texttt{start()}}\\readiness, timers and callbacks\\run on the caller thread};

\end{tikzpicture}
\end{document}
