\documentclass[tikz,border=8pt]{standalone}
\usepackage[T1]{fontenc}
\usepackage{lmodern}
\input{landingpages-figure-system.tex}
\begin{document}
\begin{tikzpicture}[mqtt desktop]

\node[mqtt config,mqtt node wide] (root) {\texttt{ConfigRoot}\\application configuration root};

\node[mqtt section title,below=\MQSectionSeparation of root] (namedtitle)
  {Named children of \texttt{ConfigRoot}};
\node[mqtt config,below=\MQGapS of namedtitle,mqtt node compact] (instancea)
  {named endpoint\\instance A};
\node[mqtt application,left=\MQHorizontalGapTight of instancea,mqtt node compact] (app)
  {application-owned\\\texttt{SubCommand}};
\node[mqtt config,right=\MQHorizontalGapTight of instancea,mqtt node compact] (instanceb)
  {named endpoint\\instance B};
\node[mqtt layout fit,fit=(namedtitle)(app)(instancea)(instanceb)] (childrenfit) {};

\node[mqtt section title,below=\MQSectionSeparation of childrenfit,anchor=north] (sectionstitle)
  {Instance A sections};
\node[mqtt config,below=\MQGapS of sectionstitle,mqtt node compact] (local) {\texttt{local}};
\node[mqtt config,left=\MQHorizontalGapTight of local,mqtt node compact] (remote) {\texttt{remote}};
\node[mqtt config,right=\MQHorizontalGapTight of local,mqtt node compact] (connection) {\texttt{connection}};

\path (remote.south) -- (local.south) coordinate[midway] (lowerleftaxis);
\path (local.south) -- (connection.south) coordinate[midway] (lowerrightaxis);
\node[mqtt config,below=\MQGapS of lowerleftaxis,mqtt node compact] (socket) {\texttt{socket}};
\node[mqtt config,below=\MQGapS of lowerrightaxis,mqtt node compact] (tls)
  {\texttt{tls}\\when selected};
\node[mqtt layout fit,fit=(sectionstitle)(remote)(local)(connection)(socket)(tls)] (sectionsfit) {};

\node[mqtt note,below=\MQGapXL of sectionsfit,mqtt node extra wide] (dependent)
  {Section availability is role/family/mode dependent;\\the figure shows the reusable\\section vocabulary.};
\node[mqtt note,below=\MQGapM of dependent,mqtt node hero] (anonymous)
  {Anonymous endpoint instance: configurable through the C++ API,\\but not a named \texttt{ConfigRoot} child and therefore not addressable\\by instance name from CLI or configuration file.};

\node[mqtt group,fit=(namedtitle)(app)(instancea)(instanceb)] (children) {};
\node[mqtt group,fit=(sectionstitle)(remote)(local)(connection)(socket)(tls)] (sections) {};
\begin{scope}[on background layer]
  \node[mqtt figure frame,fit=(root)(children)(sections)(dependent)(anonymous)] {};
\end{scope}

\draw[mqtt contains] (root.south) -- node[mqtt edge label right] {contains} (children.north);
\draw[mqtt contains] (instancea.south) -- node[mqtt edge label right] {assembles} (sections.north);

\end{tikzpicture}
\end{document}
