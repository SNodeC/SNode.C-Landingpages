\documentclass[tikz,border=8pt]{standalone}
\usepackage[T1]{fontenc}
\usepackage{lmodern}
\input{mqttsystem-figure-system.tex}
\begin{document}
\begin{tikzpicture}[mqtt mobile]
\tikzset{
  half/.style={minimum width=34mm,text width=29mm},
  wide/.style={minimum width=76mm,text width=69mm}
}

\node[mqtt external,half] (direct)
  {Direct MQTT\\\texttt{in / in6 / un}\\plain / TLS};
\node[mqtt external,half,right=8mm of direct] (http)
  {HTTP / HTTPS\\\texttt{in / in6 / un}\\MQTT upgrade};
\node[mqtt process,half,below=\MQGapS of direct] (directctx)
  {per-connection\\MQTT server context};
\node[mqtt process,half,below=\MQGapS of http] (wsctx)
  {per-connection\\WebSocket + MQTT context};
\node[mqtt application,wide,below=\MQGapL of $(directctx.south)!0.5!(wsctx.south)$] (broker)
  {shared Broker core};
\node[mqtt state,wide,below=\MQGapS of broker] (state)
  {shared sessions + subscriptions + retained state};
\node[mqtt state,wide,below=\MQGapS of state] (model)
  {observable \texttt{MqttModel} state};

\draw[mqtt data flow] (direct.south) -- (directctx.north);
\draw[mqtt data flow] (http.south) -- (wsctx.north);
\draw[mqtt data flow] (directctx.south) -- ++(0,-6mm) -| \MQPortNorth{broker}{.30};
\draw[mqtt data flow] (wsctx.south) -- ++(0,-6mm) -| \MQPortNorth{broker}{.70};
\draw[mqtt flow] (broker.south) -- (state.north);
\draw[mqtt observation] (broker.east) -- ++(8mm,0) |- ($(model.east)+(8mm,0)$) -- (model.east);

\begin{pgfonlayer}{background}
  \node[mqtt figure frame,fit=(direct)(http)(directctx)(wsctx)(broker)(state)(model)] (frame) {};
  \node[mqtt data plane,fit=(direct)(http)(directctx)(wsctx)(broker)(state)] (plane) {};
\end{pgfonlayer}
\node[mqtt section title] at ($(frame.north west)+(\MQTitleGap,-\MQTitleGap)$)
  {One shared Broker core};

\end{tikzpicture}
\end{document}
