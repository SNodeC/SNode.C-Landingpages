\documentclass[tikz,border=8pt]{standalone}
\usepackage[T1]{fontenc}
\usepackage{lmodern}
\input{mqttsystem-figure-system.tex}
\begin{document}
\begin{tikzpicture}[mqtt desktop]
\tikzset{box/.style={mqtt config,minimum width=34mm,text width=30mm}}
\node[mqtt application,box] (app) {application root\\e.g. \texttt{mqttcli}};
\node[mqtt config,box,below=\MQGapS of app] (instance) {named instance\\e.g. \texttt{in-mqtt}};
\draw[mqtt flow] (app.south) -- (instance.north);
\node[mqtt config,minimum width=39mm,text width=35mm,right=\MQHorizontalGap of instance,yshift=11mm] (address) {address / transport\\remote or local};
\node[mqtt config,minimum width=39mm,text width=35mm,right=\MQHorizontalGap of instance,yshift=-11mm] (tlshttp) {TLS / HTTP layer\\when selected};
\draw[mqtt association] (instance.east) -- (address.west);
\draw[mqtt association] (instance.east) -- (tlshttp.west);
\node[mqtt config,box,right=\MQHorizontalGap of address,xshift=6mm,yshift=-11mm] (session) {MQTT session\\CONNECT settings};
\node[mqtt success,box,right=\MQHorizontalGap of session] (action) {application action\\sub / pub / store / map};
\draw[mqtt flow] (address.east) -- (session.west);
\draw[mqtt flow] (tlshttp.east) -- (session.west);
\draw[mqtt flow] (session.east) -- (action.west);
\node[mqtt note,minimum width=52mm,text width=47mm,below=17mm of instance,xshift=18mm] (surfaces) {Same tree through CLI, config files,\\\texttt{--show-config}, and command-line inspection.};
\node[mqtt note,minimum width=52mm,text width=47mm,below=17mm of session] (precedence) {Value precedence:\\API/default < config file < command line};
\begin{pgfonlayer}{background}
  \node[mqtt lane,fit=(app)(instance)(address)(tlshttp)(session)(action)(surfaces)(precedence)] (frame) {};
\end{pgfonlayer}
\node[mqtt section title] at ($(frame.north west)+(\MQTitleGap,-\MQTitleGap)$) {MQTTSuite configuration hierarchy};
\end{tikzpicture}
\end{document}
