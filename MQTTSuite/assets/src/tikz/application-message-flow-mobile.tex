\documentclass[tikz,border=8pt]{standalone}
\usepackage[T1]{fontenc}
\usepackage{lmodern}
\input{mqttsystem-figure-system.tex}
\input{snodec-canonical-figure-system.tex}
\begin{document}
\begin{tikzpicture}[mqtt mobile]

\tikzset{
  role/.style={mqtt application,minimum width=72mm,text width=66mm,minimum height=18mm},
  brokerrole/.style={mqtt control,minimum width=72mm,text width=66mm,minimum height=18mm}
}

\node[brokerrole] (broker)
  {\textbf{MQTTBroker}\\MQTT 3.1.1 brokerage + topic space\\shared session / subscription state};
\node[role,below=\MQBranchGap of broker] (integrator)
  {\textbf{MQTTIntegrator}\\transform payloads/topics and republish};
\node[role,below=\MQBranchGap of integrator] (bridge)
  {\textbf{MQTTBridge}\\forward selected publications between broker domains};
\node[role,below=\MQBranchGap of bridge] (cli)
  {\textbf{MQTTCli}\\publish, subscribe, and inspect traffic};
\node[role,below=\MQBranchGap of cli] (store)
  {\textbf{MQTTStore}\\persist raw MQTT envelopes and optional projections};

\node[mqtt note,below=\MQBranchGap of store,minimum width=72mm,text width=66mm] (note)
  {Five separate executables; compose only the roles a deployment needs.};

% Reserve a canonical major-gap title band above the first content node.  The
% figure-frame title is placed inside this band and can no longer collide with
% the Broker card at mobile width.
\coordinate (titleguard) at ($(broker.north)+(0,\MQGapLarge)$);
\begin{pgfonlayer}{background}
  \node[mqtt figure frame,fit=(titleguard)(broker)(integrator)(bridge)(cli)(store)(note)] (frame) {};
\end{pgfonlayer}
\node[mqtt section title] at ($(frame.north west)+(\MQTitleGap,-\MQTitleGap)$)
  {Five applications, five responsibilities};

\end{tikzpicture}
\end{document}
