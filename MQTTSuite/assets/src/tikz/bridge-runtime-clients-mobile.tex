\documentclass[tikz,border=0pt]{standalone}
\usepackage[T1]{fontenc}
\usepackage{lmodern}
\input{mqttsystem-figure-system.tex}
\input{snodec-canonical-figure-system.tex}
\begin{document}
\begin{tikzpicture}[mqtt mobile]
\tikzset{
  wide/.style={mqtt process,minimum width=76mm,text width=68mm},
  member/.style={mqtt process,minimum width=35mm,text width=29mm,minimum height=22mm},
  client/.style={mqtt process,minimum width=35mm,text width=29mm,minimum height=18mm},
  foot/.style={mqtt note,minimum width=76mm,text width=68mm}
}
\def\MQArrowSpan{8.3mm}
\def\MQHalfArrowSpan{4.15mm}

\node[mqtt external,wide] (json)
  {bridge-definition JSON\\\texttt{bridges[]}};
\node[mqtt control,wide,below=\MQArrowSpan of json] (store)
  {\texttt{BridgeStore}\\parse + validate + apply defaults};
\node[mqtt process,wide,below=\MQArrowSpan of store] (bridge)
  {\textbf{logical Bridge}\\name + prefix + disabled state};
\draw[mqtt control flow] (json.south) -- (store.north);
\draw[mqtt control flow] (store.south) -- (bridge.north);

\node[member,below=\MQArrowSpan of bridge,xshift=-21.65mm] (m1)
  {member A\\network + MQTT\\session + topics\\prefix + disabled};
\node[member,below=\MQArrowSpan of bridge,xshift=21.65mm] (m2)
  {member B / …\\same member model};
\coordinate (memberBus) at ($(bridge.south)+(0,-\MQHalfArrowSpan)$);
\draw[mqtt contains] (bridge.south) -- (memberBus);
\draw[mqtt contains] (m1.north |- memberBus) -- (m2.north |- memberBus);
\draw[mqtt contains] (m1.north |- memberBus) -- (m1.north);
\draw[mqtt contains] (m2.north |- memberBus) -- (m2.north);

\node[client,below=\MQArrowSpan of m1] (c1)
  {SNode.C outbound\\MQTT client A};
\node[client,below=\MQArrowSpan of m2] (c2)
  {SNode.C outbound\\MQTT client B / …};
\draw[mqtt control flow] (m1.south) -- (c1.north);
\draw[mqtt control flow] (m2.south) -- (c2.north);

\node[foot,below=\MQGapM of c1,xshift=21.65mm] (gate)
  {\textbf{Runtime creation gate}\\Bridge + member enabled; selected transport/protocol/encryption path implemented and compiled. Schema-valid choices can still be unavailable.};
\node[foot,below=\MQGapS of gate] (connack)
  {\textbf{After successful CONNACK}\\client joins its logical Bridge and SUBSCRIBEs to the member's configured filters.};

\coordinate (titleY) at ($(json.north)+(0,\MQTitleGap)$);
\node[mqtt section title,anchor=south west] (figureTitle)
  at (gate.west |- titleY) {Definition → runtime clients};
\begin{pgfonlayer}{background}
  \node[mqtt figure frame,fit=(figureTitle)(json)(store)(bridge)(m1)(m2)(c1)(c2)(gate)(connack)] (frame) {};
\end{pgfonlayer}
\end{tikzpicture}
\end{document}
