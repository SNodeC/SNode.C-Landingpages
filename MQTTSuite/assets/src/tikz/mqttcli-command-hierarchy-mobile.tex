\documentclass[tikz,border=8pt]{standalone}
\usepackage[T1]{fontenc}
\usepackage{lmodern}
\input{mqttsystem-figure-system.tex}
\input{snodec-canonical-figure-system.tex}
\begin{document}
\begin{tikzpicture}[mqtt mobile]
\tikzset{
  rootbox/.style={mqtt application,minimum width=72mm,text width=64mm,minimum height=14mm},
  section/.style={mqtt config,minimum width=35mm,text width=29mm,minimum height=20mm},
  foot/.style={mqtt note,minimum width=78mm,text width=70mm,minimum height=18mm}
}

\node[rootbox] (root) {\texttt{mqttcli}\\application root};

\node[mqtt annotation,font=\MQAnnotationFont\bfseries,below=12.8mm of root] (instance-label)
  {enabled connection instance\\sibling sections — no nesting};

\node[section,below=\MQGapS of instance-label,xshift=-20.5mm] (remote)
  {\texttt{remote}\\host + port\\or Unix path};
\node[section,right=\MQHorizontalGapTight of remote] (session)
  {\texttt{session}\\CONNECT fields\\+ default QoS};

\node[section,below=\MQGapS of remote] (http)
  {\texttt{http}\\WebSocket\\target only};
\node[section,right=\MQHorizontalGapTight of http] (sub)
  {\texttt{sub}\\one or more\\topic filters};

\node[section,below=\MQGapS of http,xshift=20.5mm] (pub)
  {\texttt{pub}\\topic + message\\+ retain};

\node[foot,below=9mm of pub] (note)
  {\texttt{sub} and \texttt{pub} may coexist. \texttt{http} is WebSocket-only. This shows configuration ownership, not runtime order.};

\coordinate (titlepad) at ($(root.north)+(0,8mm)$);

\begin{pgfonlayer}{background}
  \node[mqtt figure frame,fit=(titlepad)(root)(instance-label)(remote)(session)(http)(sub)(pub)(note)] (frame) {};
  \node[mqtt group,fit=(instance-label)(remote)(session)(http)(sub)(pub)] (instance) {};
\end{pgfonlayer}

\draw[mqtt contains] (root.south) -- (instance.north);

\node[mqtt section title] at ($(frame.north west)+(\MQTitleGap,-\MQTitleGap)$)
  {MQTTCli connection-instance configuration};

\end{tikzpicture}
\end{document}
