\documentclass[tikz,border=8pt]{standalone}
\usepackage[T1]{fontenc}
\usepackage{lmodern}
\input{mqttsystem-figure-system.tex}
\input{snodec-canonical-figure-system.tex}
\begin{document}
\begin{tikzpicture}[mqtt mobile]
\tikzset{wide/.style={minimum width=74mm,text width=66mm}}

\node[mqtt external,wide] (listener) {Broker HTTP / HTTPS listener family};
\node[mqtt control,wide,below=\MQGapS of listener] (router) {shared Express router — request dispatch};
\draw[mqtt control flow] (listener.south) -- (router.north);

\node[mqtt process,wide,below=22mm of router] (dash)
  {Dashboard / static assets — \texttt{/clients/*}\\non-upgrade \texttt{/} redirects to \texttt{/clients}};
\node[mqtt process,wide,below=\MQGapS of dash] (sse)
  {SSE — \texttt{GET /api/mqtt/events}, legacy \texttt{GET /sse}\\current state + live events from \texttt{MqttModel}};
\node[mqtt process,wide,below=\MQGapS of sse] (admin)
  {JSON administration → Broker\\\texttt{POST /api/mqtt/}: disconnect · subscribe · unsubscribe · release};
\node[mqtt data,wide,below=\MQGapS of admin] (ws)
  {MQTT WebSocket → Broker — \texttt{/ · /ws · /mqtt}\\requested subprotocol \texttt{mqtt}};

\coordinate (routesTitleGuard) at ($(dash.north)+(0,\MQGapL)$);
\begin{pgfonlayer}{background}
  \node[mqtt group,fit=(routesTitleGuard)(dash)(sse)(admin)(ws)] (routes) {};
\end{pgfonlayer}
\node[mqtt section title] at ($(routes.north west)+(\MQTitleGap,-\MQTitleGap)$)
  {Routes on the shared router};
\draw[mqtt control flow] (router.south) -- (routes.north);

\node[mqtt control,wide,below=22mm of routes] (broker)
  {Broker singleton + MQTT handlers\\MQTT data path + selected administration effects};
\node[mqtt state,wide,below=\MQGapS of broker] (model)
  {\texttt{MqttModel} — observable client / subscription / retained state\\+ SSE event source};
\draw[mqtt observation] (broker.south) -- node[mqtt edge label,right]{connection / state events} (model.north);

\coordinate (ownersTitleGuard) at ($(broker.north)+(0,\MQGapL)$);
\begin{pgfonlayer}{background}
  \node[mqtt group,fit=(ownersTitleGuard)(broker)(model)] (owners) {};
\end{pgfonlayer}
\node[mqtt section title] at ($(owners.north west)+(\MQTitleGap,-\MQTitleGap)$)
  {Runtime ownership};

\node[mqtt note,wide,below=\MQGapM of owners] (note)
  {Dashboard JavaScript consumes the administration/SSE routes;\\MQTT-over-WebSocket is a separate MQTT data route.};

\coordinate (titleguard) at ($(listener.north)+(0,\MQGapL)$);
\begin{pgfonlayer}{background}
  \node[mqtt figure frame,fit=(titleguard)(listener)(router)(routes)(owners)(note)] (frame) {};
\end{pgfonlayer}
\node[mqtt section title] at ($(frame.north west)+(\MQTitleGap,-\MQTitleGap)$)
  {Broker HTTP surfaces};

\end{tikzpicture}
\end{document}
