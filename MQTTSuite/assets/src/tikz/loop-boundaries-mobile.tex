\documentclass[tikz,border=0pt]{standalone}
\usepackage[T1]{fontenc}
\usepackage{lmodern}
\input{mqttsystem-figure-system.tex}
\input{snodec-canonical-figure-system.tex}
\begin{document}
\begin{tikzpicture}[mqtt mobile]
\tikzset{
  boundary/.style={minimum width=72mm,text width=66mm},
  qualifier/.style={mqtt note,minimum width=72mm,text width=66mm}
}

\node[mqtt control,boundary] (pass)
  {{\MQAnnotationFont\bfseries forwarding pass}\\[1mm]
   \textbf{Immediate source exclusion}\\[2mm]
   \texttt{Bridge::publish()} excludes the origin member from its destination loop.};
\node[qualifier,below=\MQGapS of pass] (passnote)
  {Scope: one forwarding pass. The origin member is not a destination in that pass.};

\node[mqtt warning,boundary,below=\MQMobileLaneGap of passnote] (topology)
  {{\MQAnnotationFont\bfseries topology}\\[1mm]
   \textbf{Topology / subscription design}\\[2mm]
   A later A $\rightarrow$ B $\rightarrow$ A cycle can still occur when forwarded topics match another member's input subscriptions.};
\node[qualifier,below=\MQGapS of topology] (topologynote)
  {Operator controls: subscription partitioning, prefix design, and topology.};

\node[mqtt control,boundary,below=\MQMobileLaneGap of topologynote] (private)
  {{\MQAnnotationFont\bfseries broker reflection}\\[1mm]
   \textbf{Private SNode.C suppression}\\[2mm]
   \texttt{loop\_prevention=true} sets private \texttt{0x80} in the MQTT CONNECT protocol-level byte.};
\node[qualifier,below=\MQGapS of private] (privatenote)
  {An SNode.C broker reads the bit as \texttt{reflect=false} and suppresses delivery back to that origin client ID.};

\node[mqtt note,minimum width=72mm,text width=66mm,below=\MQMobileLaneGap of privatenote] (limit)
  {\textbf{Independent, not sequential.}\\[1mm]The private CONNECT bit is non-standard MQTT and depends on broker support; no single boundary makes arbitrary cyclic or third-party topologies loop-safe.};

\coordinate (titleGuard) at ($(pass.north)+(0,8mm)$);
\begin{pgfonlayer}{background}
  \node[mqtt figure frame,fit=(titleGuard)(pass)(passnote)(topology)(topologynote)(private)(privatenote)(limit)] (frame) {};
\end{pgfonlayer}
\node[mqtt section title] at ($(frame.north west)+(\MQTitleGap,-\MQTitleGap)$)
  {Independent loop safeguards and limits};

\end{tikzpicture}
\end{document}
