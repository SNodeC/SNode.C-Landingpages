\documentclass[tikz,border=0pt]{standalone}
\usepackage[T1]{fontenc}
\usepackage{lmodern}
\input{mqttsystem-figure-system.tex}
\input{snodec-canonical-figure-system.tex}
\begin{document}
\begin{tikzpicture}[mqtt desktop]
\tikzset{
  surface/.style={mqtt process,minimum width=50mm,text width=44mm},
  route/.style={mqtt data,minimum width=50mm,text width=44mm},
  owner/.style={minimum width=52mm,text width=46mm}
}

\node[mqtt external,minimum width=48mm,text width=42mm] (listener)
  {Broker HTTP / HTTPS listener family};
\node[mqtt control,minimum width=48mm,text width=42mm,right=\MQGapM of listener] (router)
  {shared Express router\\request dispatch};
\draw[mqtt control flow] (listener.east) -- (router.west);

\node[surface,below=22mm of listener] (dash)
  {Dashboard / static assets\\\texttt{/clients/*}\\non-upgrade \texttt{/} redirects to \texttt{/clients}};
\node[surface,right=\MQGapM of dash] (sse)
  {SSE current state + live events\\\texttt{GET /api/mqtt/events}\\legacy \texttt{GET /sse}\\source: \texttt{MqttModel}};
\node[surface,below=\MQGapM of dash] (admin)
  {JSON administration → Broker\\\texttt{POST /api/mqtt/}\\\texttt{disconnect · subscribe · unsubscribe · release}};
\node[route,below=\MQGapM of sse] (ws)
  {MQTT WebSocket → Broker\\\texttt{/ · /ws · /mqtt}\\requested subprotocol \texttt{mqtt}};

\coordinate (routesTitleGuard) at ($(dash.north)+(0,\MQGapL)$);
\begin{pgfonlayer}{background}
  \node[mqtt group,fit=(routesTitleGuard)(dash)(sse)(admin)(ws)] (routes) {};
\end{pgfonlayer}
\node[mqtt section title] at ($(routes.north west)+(\MQTitleGap,-\MQTitleGap)$)
  {Routes installed on the shared HTTP router};
\draw[mqtt control flow] (router.south) -- (routes.north);

\node[mqtt control,owner,below=22mm of routes,xshift=-29mm] (broker)
  {Broker singleton + MQTT handlers\\MQTT data path + selected admin effects};
\node[mqtt state,owner,below=22mm of routes,xshift=29mm] (model)
  {\texttt{MqttModel}\\observable client / subscription / retained state\\+ SSE event source};
\draw[mqtt observation] (broker.east) -- node[mqtt edge label,above]{connection / state events} (model.west);

\coordinate (ownersTitleGuard) at ($(broker.north)+(0,\MQGapL)$);
\begin{pgfonlayer}{background}
  \node[mqtt group,fit=(ownersTitleGuard)(broker)(model)] (owners) {};
\end{pgfonlayer}
\node[mqtt section title] at ($(owners.north west)+(\MQTitleGap,-\MQTitleGap)$)
  {Runtime ownership};

\node[mqtt note,below=\MQGapM of owners,minimum width=110mm,text width=104mm] (note)
  {The bundled dashboard is a browser client of the administration/SSE routes. MQTT-over-WebSocket is a separate MQTT data route through the same HTTP server infrastructure.};

\coordinate (titleguard) at ($(listener.north)+(0,\MQGapL)$);
\begin{pgfonlayer}{background}
  \node[mqtt figure frame,fit=(titleguard)(listener)(router)(routes)(owners)(note)] (frame) {};
\end{pgfonlayer}
\node[mqtt section title] at ($(frame.north west)+(\MQTitleGap,-\MQTitleGap)$)
  {Broker HTTP surfaces};

% Authoritative SNode.C desktop canvas: exactly 160 mm wide, native drawing.
\coordinate (snodecCanvasCenter) at (current bounding box.center);
\coordinate (snodecCanvasSW)
  at ($(snodecCanvasCenter)+(-80mm,0)$ |- current bounding box.south);
\coordinate (snodecCanvasNE)
  at ($(snodecCanvasCenter)+(80mm,0)$ |- current bounding box.north);
\path[use as bounding box]
  ([yshift=-7pt]snodecCanvasSW) rectangle ([yshift=7pt]snodecCanvasNE);

\end{tikzpicture}
\end{document}
