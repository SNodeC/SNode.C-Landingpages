\documentclass[tikz,border=8pt]{standalone}
\usepackage[T1]{fontenc}
\usepackage{lmodern}
\input{mqttsystem-figure-system.tex}
\begin{document}
\begin{tikzpicture}[mqtt desktop]
\tikzset{box/.style={mqtt process,minimum width=40mm,text width=35mm}}

\node[mqtt external,box] (brokers) {remote broker members};
\node[mqtt data,box,right=\MQHorizontalGapLoose of brokers] (clients)
  {outbound MQTT member clients};
\node[mqtt application,box,right=\MQHorizontalGapLoose of clients] (core)
  {MQTTBridge\\logical bridges};

\draw[mqtt data flow] \MQPortEast{brokers}{.70} -- node[mqtt edge label,above]{selected PUBLISH} \MQPortWest{clients}{.70};
\draw[mqtt data flow] \MQPortEast{clients}{.70} -- \MQPortWest{core}{.70};
\draw[mqtt data flow] \MQPortWest{core}{.30} -- \MQPortEast{clients}{.30};
\draw[mqtt data flow] \MQPortWest{clients}{.30} -- node[mqtt edge label,below]{forwarded PUBLISH} \MQPortEast{brokers}{.30};

\node[mqtt external,minimum width=40mm,text width=35mm,right=20mm of core,yshift=20mm] (operator)
  {browser / operator};
\node[mqtt control,minimum width=52mm,text width=47mm,below=\MQGapS of operator] (admin)
  {HTTP / HTTPS operator surface\\GET config + PATCH config + SSE};
\node[mqtt warning,minimum width=65mm,text width=59mm,below=\MQGapS of admin] (risk)
  {No application authentication. GET can return broker credentials. PATCH mutates/stages configuration and triggers client restart processing.};
\node[mqtt note,minimum width=65mm,text width=59mm,below=\MQGapS of risk] (tls)
  {HTTPS protects transport only. Protect configuration read-back, mutation, and SSE with trusted network policy or external access control.};
\draw[mqtt control flow] (operator.south) -- (admin.north);
\draw[mqtt association] (core.east) -- ++(8mm,0) |- (admin.west);
\draw[mqtt association] (admin.south) -- (risk.north);
\draw[mqtt association] (risk.south) -- (tls.north);

\begin{pgfonlayer}{background}
  \node[mqtt figure frame,fit=(brokers)(clients)(core)(operator)(admin)(risk)(tls)] (frame) {};
  \node[mqtt trust boundary,fit=(operator)(admin)(risk)(tls)] (trust) {};
\end{pgfonlayer}
\node[mqtt section title] at ($(frame.north west)+(\MQTitleGap,-\MQTitleGap)$)
  {MQTTBridge administration exposure};
\node[mqtt annotation,anchor=west,fill=MQSurface] at ($(trust.north west)+(\MQTitleGap,0)$)
  {required external / trusted deployment boundary — not enforced by MQTTBridge};

\end{tikzpicture}
\end{document}
