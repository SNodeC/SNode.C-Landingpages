\documentclass[tikz,border=8pt]{standalone}
\usepackage[T1]{fontenc}
\usepackage{lmodern}
\input{mqttsystem-figure-system.tex}
\input{snodec-canonical-figure-system.tex}
\begin{document}
\begin{tikzpicture}[mqtt mobile]
\tikzset{
  wide/.style={minimum width=72mm,text width=64mm},
  peer/.style={minimum width=32mm,text width=27mm}
}

\node[mqtt external,wide] (a) {Broker A — source domain};
\node[mqtt application,wide,below=\MQGapS of a] (ma)
  {member A — configured subscriptions select received PUBLISH};
\node[mqtt control,wide,below=\MQGapM of ma] (bridge)
  {logical bridge — fan-out forwarding};
\node[mqtt state,wide,below=\MQGapM of bridge] (fanout)
  {connected destination set\\all currently connected bridge members\\except origin member A};

\draw[mqtt data flow] (a.south) -- (ma.north);
\draw[mqtt data flow] (ma.south) -- (bridge.north);
\draw[mqtt data flow] (bridge.south) -- (fanout.north);

\node[mqtt application,peer,below=\MQGapM of fanout,xshift=-20mm] (mb) {member B};
\node[mqtt application,peer,below=\MQGapM of fanout,xshift=20mm] (mc) {member C};
\node[mqtt external,peer,below=\MQGapS of mb] (b) {Broker B};
\node[mqtt external,peer,below=\MQGapS of mc] (c) {Broker C};

\coordinate (fanoutB) at ($(fanout.south west)!.333333!(fanout.south east)$);
\coordinate (fanoutC) at ($(fanout.south west)!.666667!(fanout.south east)$);
\coordinate (fanoutMidB) at ($(fanoutB)!0.5!(mb.north)$);
\coordinate (fanoutMidC) at ($(fanoutC)!0.5!(mc.north)$);
\draw[mqtt data flow] (fanoutB) -- (fanoutB |- fanoutMidB) -| (mb.north);
\draw[mqtt data flow] (fanoutC) -- (fanoutC |- fanoutMidC) -| (mc.north);
\draw[mqtt data flow] (mb.south) -- (b.north);
\draw[mqtt data flow] (mc.south) -- (c.north);

\coordinate (brokersMid) at ($(b.south)!0.5!(c.south)$);
\node[mqtt note,wide,below=\MQGapM of brokersMid] (note)
  {message bytes, received QoS, and retain are preserved;\\the forwarded topic is rebuilt from bridge/member prefixes;\\construction is documented separately};

\coordinate (titleguard) at ($(a.north)+(0,\MQGapL)$);
\begin{pgfonlayer}{background}
  \node[mqtt figure frame,fit=(titleguard)(a)(ma)(bridge)(fanout)(mb)(mc)(b)(c)(note)] (frame) {};
\end{pgfonlayer}
\node[mqtt section title] at ($(frame.north west)+(\MQTitleGap,-\MQTitleGap)$)
  {Logical bridge forwarding};

\end{tikzpicture}
\end{document}
