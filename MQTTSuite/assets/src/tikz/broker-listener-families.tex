\documentclass[tikz,border=8pt]{standalone}
\usepackage[T1]{fontenc}
\usepackage{lmodern}
\input{mqttsystem-figure-system.tex}
\begin{document}
\begin{tikzpicture}[mqtt desktop]
\tikzset{
  family/.style={mqtt group,minimum width=48mm},
  core/.style={mqtt application,minimum width=48mm,text width=43mm},
  statebox/.style={mqtt state,minimum width=43mm,text width=39mm}
}

\node[mqtt external,minimum width=45mm,text width=41mm] (direct)
  {Direct MQTT listeners\\\texttt{in / in6 / un}\\plain or TLS stream};
\node[mqtt external,minimum width=45mm,text width=41mm,below=\MQGapL of direct] (http)
  {HTTP / HTTPS listeners\\\texttt{in / in6 / un}\\dashboard + MQTT upgrade};

\node[mqtt process,minimum width=40mm,text width=36mm,right=\MQHorizontalGapLoose of direct] (directctx)
  {per-connection\\MQTT server context};
\node[mqtt process,minimum width=40mm,text width=36mm,right=\MQHorizontalGapLoose of http] (wsctx)
  {HTTP upgrade to\\WebSocket + MQTT context};

\node[core,right=\MQHorizontalGapLoose of directctx,yshift=-18mm] (broker)
  {shared Broker core\\session routing + subscriptions};
\node[statebox,right=\MQHorizontalGapLoose of broker] (state)
  {shared broker state\\sessions\\subscriptions\\retained messages};
\node[mqtt state,minimum width=48mm,text width=44mm,below=\MQGapM of state] (model)
  {MQTTSuite observable state\\\texttt{MqttModel}};

\draw[mqtt data flow] (direct.east) -- (directctx.west);
\draw[mqtt data flow] (http.east) -- (wsctx.west);
\draw[mqtt data flow] (directctx.east) -| (broker.north west);
\draw[mqtt data flow] (wsctx.east) -| (broker.south west);
\draw[mqtt flow] (broker.east) -- (state.west);
\draw[mqtt observation] (broker.east) |- (model.west);

\begin{pgfonlayer}{background}
  \node[mqtt data plane,fit=(direct)(http)(directctx)(wsctx)(broker)(state)] (plane) {};
  \node[mqtt lane,fit=(plane)(model)] (frame) {};
\end{pgfonlayer}
\node[mqtt section title] at ($(frame.north west)+(\MQTitleGap,-\MQTitleGap)$)
  {Listener families converge on one Broker core};
\node[mqtt annotation,anchor=east] at ($(frame.north east)+(-\MQTitleGap,-\MQTitleGap)$)
  {all MQTT clients participate in common broker/session state};

\end{tikzpicture}
\end{document}
