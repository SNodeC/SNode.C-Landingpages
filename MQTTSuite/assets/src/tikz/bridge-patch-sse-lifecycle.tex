\documentclass[tikz,border=8pt]{standalone}
\usepackage[T1]{fontenc}
\usepackage{lmodern}
\input{mqttsystem-figure-system.tex}

\begin{document}
\begin{tikzpicture}[mqtt desktop]
\tikzset{
  life/.style={mqtt process,minimum width=34mm,text width=29mm},
  controlbox/.style={mqtt control,minimum width=34mm,text width=29mm},
  eventbox/.style={mqtt process,minimum width=40mm,text width=35mm}
}

% ---------------------------------------------------------------------------
% HTTP administration: the response is sent before close/restart processing.
% ---------------------------------------------------------------------------

\node[life] (h0) {Operator};
\node[life,right=\MQHorizontalGap of h0] (h1)
  {PATCH\\\texttt{/api/bridge/config}};
\node[mqtt decision,minimum width=31mm,text width=26mm,right=\MQHorizontalGap of h1] (h2)
  {restart\\already active?};
\node[controlbox,right=\MQHorizontalGap of h2] (h3)
  {apply JSON Patch\\validate + defaults\\stage definition};
\node[mqtt success,minimum width=36mm,text width=31mm,right=\MQHorizontalGap of h3] (h4)
  {HTTP 200\\patch accepted};

\node[mqtt error,minimum width=34mm,text width=29mm,below=\MQBranchGapTight of h2] (h409)
  {HTTP 409\\restart active\\patch not applied};
\node[mqtt error,minimum width=42mm,text width=37mm,below=\MQBranchGapTight of h3] (hbad)
  {HTTP 400 / 404\\request attribute missing\\or patch / validation failure};

\draw[mqtt flow] (h0.east) -- (h1.west);
\draw[mqtt flow] (h1.east) -- (h2.west);
\draw[mqtt flow] (h2.east) -- node[mqtt branch label,above]{NO} (h3.west);
\draw[mqtt flow] (h3.east) -- (h4.west);
\draw[mqtt flow] (h2.south) -- node[mqtt branch label,right]{YES} (h409.north);
\draw[mqtt flow] (h3.south) -- (hbad.north);

% ---------------------------------------------------------------------------
% Runtime restart: restartBridges() is immediate only when no flow remains;
% otherwise the final flow-completion callback resumes it.
% ---------------------------------------------------------------------------

\node[controlbox,below=45mm of h0] (r0)
  {\texttt{closeBridges()}\\mark restart\\emit stopping events when flows exist\\send DISCONNECT + terminate flows};
\node[mqtt decision,minimum width=31mm,text width=26mm,right=\MQHorizontalGap of r0] (r1)
  {active flows\\remain?};
\node[controlbox,right=\MQHorizontalGap of r1] (r2)
  {\texttt{restartBridges()}\\resume restart};
\node[controlbox,right=\MQHorizontalGap of r2] (r3)
  {\texttt{activateStaged()}\\active-definition write attempt\\rebuild logical bridges};
\node[controlbox,right=\MQHorizontalGap of r3] (r4)
  {\texttt{startBridges()}\\\texttt{bridges\_starting}\\create / connect enabled clients};

\node[mqtt note,minimum width=42mm,text width=37mm,below=\MQBranchGapTight of r1] (wait)
  {YES: wait for final flow-completion callback};
\draw[mqtt control flow] (r0.east) -- (r1.west);
\draw[mqtt control flow] (r1.east) -- node[mqtt branch label,above]{NO — immediate} (r2.west);
\draw[mqtt control flow] (r1.south) -- node[mqtt branch label,right]{YES} (wait.north);
\draw[mqtt control flow] (wait.east) -- ++(8mm,0) |- node[mqtt edge label,near end,below]{final completion} \MQPortSouth{r2}{.50};
\draw[mqtt control flow] (r2.east) -- (r3.west);
\draw[mqtt control flow] (r3.east) -- (r4.west);

\draw[mqtt handoff] (h4.south) -- ++(0,-8mm) -| node[mqtt edge label,near start,right,text=MQCrossLane]{response already sent; restart processing begins} (r0.north);

% ---------------------------------------------------------------------------
% SSE: runtime emits events; the observer is the sink.
% ---------------------------------------------------------------------------

\node[eventbox,below=45mm of r0] (s0)
  {when flows exist:\\\texttt{bridges\_stopping}\\\texttt{bridge\_stopping}\\\texttt{broker\_disconnecting}};
\node[eventbox,right=\MQHorizontalGap of s0] (s1)
  {at \texttt{startBridges()}:\\\texttt{bridges\_starting}\\clears replay buffer};
\node[eventbox,right=\MQHorizontalGap of s1] (s2)
  {startup / settlement:\\\texttt{bridge\_starting}\\\texttt{broker\_connecting} / disabled\\connected / started events};
\node[mqtt external,minimum width=36mm,text width=31mm,right=\MQHorizontalGap of s2] (observer)
  {SSE observer};

\draw[mqtt observation] (s0.east) -- (s1.west);
\draw[mqtt observation] (s1.east) -- (s2.west);
\draw[mqtt observation] (s2.east) -- (observer.west);

\draw[mqtt observation] \MQPortSouth{r0}{.35} -- ++(0,-8mm) -| (s0.north);
\draw[mqtt observation] \MQPortSouth{r4}{.35} -- ++(0,-8mm) -| \MQPortNorth{s1}{.65};
\draw[mqtt observation] \MQPortSouth{r4}{.70} -- ++(0,-16mm) -| \MQPortNorth{s2}{.65};

\node[mqtt note,minimum width=132mm,text width=125mm,below=\MQGapM of s1,xshift=42mm] (replay)
  {Replay contains events retained since the current \texttt{bridges\_starting} boundary; preceding stopping events are cleared at that boundary. \texttt{Last-Event-ID} is ignored. Each connected receiver gets an SSE keep-alive every 39 s.};

% ---------------------------------------------------------------------------
% Frame and semantic lane boundaries. Neutral frame is painted first.
% ---------------------------------------------------------------------------

\begin{pgfonlayer}{background}
  \node[mqtt figure frame,fit=(h0)(h1)(h2)(h3)(h4)(h409)(hbad)(r0)(r1)(r2)(r3)(r4)(wait)(s0)(s1)(s2)(observer)(replay)] (frame) {};
  \node[mqtt lane,fit=(h0)(h1)(h2)(h3)(h4)(h409)(hbad)] (lane-http) {};
  \node[mqtt lane,fit=(r0)(r1)(r2)(r3)(r4)(wait)] (lane-runtime) {};
  \node[mqtt lane,fit=(s0)(s1)(s2)(observer)(replay)] (lane-sse) {};
\end{pgfonlayer}

\node[mqtt section title] at ($(lane-http.north west)+(\MQTitleGap,-\MQTitleGap)$)
  {HTTP administration};
\node[mqtt section title] at ($(lane-runtime.north west)+(\MQTitleGap,-\MQTitleGap)$)
  {Runtime restart lifecycle};
\node[mqtt section title] at ($(lane-sse.north west)+(\MQTitleGap,-\MQTitleGap)$)
  {SSE observation};
\node[mqtt annotation,anchor=east] at ($(lane-sse.north east)+(-\MQTitleGap,-\MQTitleGap)$)
  {dashed arrows = lifecycle events emitted toward observers};

\end{tikzpicture}
\end{document}
