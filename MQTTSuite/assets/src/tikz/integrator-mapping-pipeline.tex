\documentclass[tikz,border=0pt]{standalone}
\usepackage[T1]{fontenc}
\usepackage{lmodern}
\input{mqttsystem-figure-system.tex}
\input{snodec-canonical-figure-system.tex}
\begin{document}
\begin{tikzpicture}[mqtt desktop]
\tikzset{
  stage/.style={minimum width=39mm,text width=33mm},
  note/.style={mqtt note,minimum width=48mm,text width=42mm}
}

\node[mqtt external,stage] (brokerin)
  {Broker delivers a PUBLISH matching a mapping-derived subscription};
\node[mqtt application,stage,right=\MQHorizontalGap of brokerin] (client)
  {MQTTIntegrator\\MQTT client connection};
\node[mqtt control,stage,right=\MQHorizontalGap of client] (match)
  {topic-tree match\\literal / \texttt{+} / terminal \texttt{\#}};
\draw[mqtt data flow] (brokerin.east) -- (client.west);
\draw[mqtt data flow] (client.east) -- (match.west);

\node[mqtt control,stage,below=\MQGapM of match] (rules)
  {apply mapping modes\\static / value / JSON};
\node[mqtt process,stage,left=\MQHorizontalGap of rules] (outputs)
  {produce $0..n$ mapped PUBLISH messages\\immediate and/or scheduled};
\node[mqtt data,stage,left=\MQHorizontalGap of outputs] (send)
  {send mapped PUBLISH\\through the same MQTT client connection};
\draw[mqtt control flow] (match.south) -- (rules.north);
\draw[mqtt control flow] (rules.west) -- (outputs.east);
\draw[mqtt data flow] (outputs.west) -- (send.east);
\draw[mqtt data flow] (send.north) -- ++(0,6mm) -| \MQPortSouth{client}{.28};

\node[note,below=\MQGapM of send] (subqos)
  {Subscription QoS comes from \texttt{subscription.qos} when the mapping-derived subscription is created.};
\node[note,below=\MQGapM of rules] (pubqos)
  {Each mapped PUBLISH carries its own output QoS and retain settings; delayed outputs use the same values when the timer fires.};
\draw[mqtt association] (brokerin.south) -- (subqos.north);
\draw[mqtt association] (outputs.south) -- (pubqos.north);

\coordinate (titleguard) at ($(brokerin.north)+(0,\MQGapL)$);
\begin{pgfonlayer}{background}
  \node[mqtt figure frame,fit=(titleguard)(brokerin)(client)(match)(rules)(outputs)(send)(subqos)(pubqos)] (frame) {};
\end{pgfonlayer}
\node[mqtt section title] at ($(frame.north west)+(\MQTitleGap,-\MQTitleGap)$)
  {MQTTIntegrator mapping pipeline};

% Authoritative SNode.C desktop canvas: exactly 160 mm wide, native drawing.
\coordinate (snodecCanvasCenter) at (current bounding box.center);
\coordinate (snodecCanvasSW) at ($(snodecCanvasCenter)+(-80mm,0)$ |- current bounding box.south);
\coordinate (snodecCanvasNE) at ($(snodecCanvasCenter)+(80mm,0)$ |- current bounding box.north);
\path[use as bounding box] ([yshift=-7pt]snodecCanvasSW) rectangle ([yshift=7pt]snodecCanvasNE);

\end{tikzpicture}
\end{document}
