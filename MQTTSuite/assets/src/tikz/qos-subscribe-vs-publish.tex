\documentclass[tikz,border=0pt]{standalone}
\usepackage[T1]{fontenc}
\usepackage{lmodern}
\input{mqttsystem-figure-system.tex}
\input{snodec-canonical-figure-system.tex}
\begin{document}
\begin{tikzpicture}[mqtt desktop]
\tikzset{
  choice/.style={minimum width=52mm,text width=46mm,minimum height=16mm},
  example/.style={mqtt config,minimum width=52mm,text width=46mm,minimum height=19mm},
  summary/.style={mqtt note,minimum width=112mm,text width=104mm,minimum height=13mm}
}

\node[mqtt control,choice] (subscribe)
  {\textbf{Subscription QoS}\\requested delivery ceiling\\for one topic filter};
\node[mqtt data,choice,right=\MQHorizontalGap of subscribe] (publish)
  {\textbf{Publish QoS}\\QoS carried by one\\outgoing PUBLISH};

\node[example,below=\MQGapS of subscribe] (integratorSub)
  {\textbf{MQTTIntegrator}\\\texttt{subscription.qos}\\sets requested SUBSCRIBE QoS};
\node[example,below=\MQGapS of publish] (integratorPub)
  {\textbf{MQTTIntegrator}\\mapping output \texttt{qos}\\sets mapped PUBLISH QoS};

\node[example,below=\MQGapS of integratorSub] (cliSub)
  {\textbf{MQTTCli}\\\texttt{-{}-qos} is the default\\filter \texttt{\#\#<qos>} overrides it};
\node[example,below=\MQGapS of integratorPub] (cliPub)
  {\textbf{MQTTCli}\\\texttt{-{}-qos} is the default\\publish topic \texttt{\#\#<qos>} overrides it};

\node[summary,below=\MQGapM of $(cliSub.south)!0.5!(cliPub.south)$] (note)
  {Without a topic override, MQTTCli uses the same session default on both sides. A filter or publish-topic \texttt{\#\#<qos>} suffix overrides only that operation; the two QoS decisions remain independent.};

\node[fit=(subscribe)(publish)(integratorSub)(integratorPub)(cliSub)(cliPub)(note)] (contentBounds) {};
\coordinate (titleY) at ($(subscribe.north)+(0,\MQTitleGap)$);
\node[mqtt section title,anchor=south west] (figureTitle)
  at (contentBounds.west |- titleY) {Subscription QoS versus publish QoS};

\begin{pgfonlayer}{background}
  \node[mqtt figure frame,fit=(figureTitle)(contentBounds)] {};
\end{pgfonlayer}
\end{tikzpicture}
\end{document}
