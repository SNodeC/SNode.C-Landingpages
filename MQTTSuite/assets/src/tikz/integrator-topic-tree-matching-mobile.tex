\documentclass[tikz,border=8pt]{standalone}
\usepackage[T1]{fontenc}
\usepackage{lmodern}
\input{mqttsystem-figure-system.tex}
\input{snodec-canonical-figure-system.tex}
\begin{document}
\begin{tikzpicture}[mqtt mobile]
\tikzset{
  widecontrol/.style={mqtt control,minimum width=72mm,text width=64mm},
  widecandidate/.style={mqtt config,minimum width=72mm,text width=64mm},
  widenote/.style={mqtt note,minimum width=72mm,text width=64mm},
  widewarning/.style={mqtt warning,minimum width=72mm,text width=64mm}
}
\def\MQArrowSpan{8.3mm}

\node[widecontrol] (root) {parent topic level: \texttt{devices}};
\node[widecontrol,below=\MQArrowSpan of root] (scan)
  {test siblings 1 → 2 → 3 in document order\\stop at the first matching branch};
\draw[mqtt control flow] (root.south) -- (scan.north);

\node[widecandidate,below=\MQArrowSpan of scan] (literal)
  {1 · literal sibling — \texttt{room-01}};
\node[widecandidate,below=\MQArrowSpan of literal] (plus)
  {2 · single-level sibling — \texttt{+}};
\node[widecandidate,below=\MQArrowSpan of plus] (hash)
  {3 · terminal multi-level sibling — \texttt{\#}};
\draw[mqtt control flow] (scan.south) -- (literal.north);
\draw[mqtt control flow] (literal.south) -- (plus.north);
\draw[mqtt control flow] (plus.south) -- (hash.north);

\node[widenote,below=\MQGapM of hash] (examples)
  {Examples: \texttt{devices/room-01} → literal;\\
   \texttt{devices/lab} → \texttt{+};\\
   \texttt{devices/lab/x} → terminal \texttt{\#}.\\
   \texttt{devices} may select a child terminal \texttt{\#} only when the matched parent has no subscription.};
\node[widewarning,below=\MQGapM of examples] (order)
  {1 → 2 → 3 is only this example's document order. There is no built-in literal → \texttt{+} → \texttt{\#} precedence.};
\node[widenote,below=\MQGapM of order] (qos)
  {Subscription QoS belongs to the selected topic branch.};

\node[mqtt section title,anchor=south west] (figureTitle)
  at ($(root.north west)+(0,\MQTitleGap)$) {Topic-tree matching};
\begin{pgfonlayer}{background}
  \node[mqtt figure frame,fit=(figureTitle)(root)(scan)(literal)(plus)(hash)(examples)(order)(qos)] (frame) {};
\end{pgfonlayer}
\end{tikzpicture}
\end{document}
