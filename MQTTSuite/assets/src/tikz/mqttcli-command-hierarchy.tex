\documentclass[tikz,border=8pt]{standalone}
\usepackage[T1]{fontenc}
\usepackage{lmodern}
\input{mqttsystem-figure-system.tex}
\input{snodec-canonical-figure-system.tex}
\begin{document}
\begin{tikzpicture}[mqtt desktop]
\tikzset{
  section/.style={mqtt config,minimum width=39mm,text width=33mm,minimum height=17mm},
  rootbox/.style={mqtt application,minimum width=49mm,text width=43mm,minimum height=15mm},
  foot/.style={mqtt note,minimum width=132mm,text width=126mm,minimum height=14mm}
}

\node[rootbox] (root) {\texttt{mqttcli}\\application root};

\node[mqtt annotation,font=\MQAnnotationFont\bfseries,below=12.8mm of root] (instance-label)
  {enabled connection instance — sibling configuration sections};

\node[section,below=\MQGapS of instance-label,xshift=-47mm] (remote)
  {\texttt{remote}\\host + port\\or Unix path};
\node[section,right=\MQHorizontalGap of remote] (session)
  {\texttt{session}\\CONNECT/session fields\\+ default QoS};
\node[section,right=\MQHorizontalGap of session] (sub)
  {\texttt{sub}\\one or more\\topic filters};

\node[section,below=\MQGapS of remote,xshift=23.5mm] (http)
  {\texttt{http}\\WebSocket target only\\(WS/WSS instances)};
\node[section,right=\MQHorizontalGap of http] (pub)
  {\texttt{pub}\\topic + message\\+ retain};

\node[foot,below=9mm of http,xshift=23.5mm] (note)
  {\texttt{sub} and \texttt{pub} may coexist. \texttt{http} appears only on WebSocket instances. This shows configuration ownership, not runtime order.};

\coordinate (titlepad) at ($(root.north)+(0,7mm)$);

\begin{pgfonlayer}{background}
  \node[mqtt figure frame,fit=(titlepad)(root)(instance-label)(remote)(session)(sub)(http)(pub)(note)] (frame) {};
  \node[mqtt group,fit=(instance-label)(remote)(session)(sub)(http)(pub)] (instance) {};
\end{pgfonlayer}

\draw[mqtt contains] (root.south) -- (instance.north);

\node[mqtt section title] at ($(frame.north west)+(\MQTitleGap,-\MQTitleGap)$)
  {MQTTCli connection-instance configuration};

\end{tikzpicture}
\end{document}
