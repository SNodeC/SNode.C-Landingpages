\documentclass[tikz,border=8pt]{standalone}
\usepackage[T1]{fontenc}
\usepackage{lmodern}
\input{mqttsystem-figure-system.tex}
\begin{document}
\begin{tikzpicture}[mqtt desktop]
\tikzset{box/.style={mqtt process,minimum width=39mm,text width=34mm}}

\node[mqtt application,box] (root) {\texttt{mqttcli}\\application root};
\node[mqtt control,box,right=\MQHorizontalGapLoose of root] (instance)
  {enabled connection instance\\direct MQTT or WebSocket};
\draw[mqtt contains] (root.east) -- (instance.west);

\node[mqtt config,box,right=36mm of instance,yshift=36mm] (remote)
  {\texttt{remote}\\host / port or Unix path};
\node[mqtt config,box,below=\MQBranchGapTight of remote] (http)
  {\texttt{http}\\WebSocket target only};
\node[mqtt config,box,below=\MQBranchGapTight of http] (session)
  {\texttt{session}\\CONNECT fields + default QoS};
\node[mqtt config,box,below=\MQBranchGapTight of session] (sub)
  {\texttt{sub}\\one or more topic filters};
\node[mqtt config,box,below=\MQBranchGapTight of sub] (pub)
  {\texttt{pub}\\topic + message + retain};

\coordinate (bus-remote) at ($(remote.west)+(-8mm,0)$);
\coordinate (bus-http) at ($(http.west)+(-8mm,0)$);
\coordinate (bus-session) at ($(session.west)+(-8mm,0)$);
\coordinate (bus-sub) at ($(sub.west)+(-8mm,0)$);
\coordinate (bus-pub) at ($(pub.west)+(-8mm,0)$);
\draw[mqtt contains] (bus-remote) -- (bus-pub);
\draw[mqtt contains] (instance.east) -- ++(8mm,0) |- (bus-session);
\draw[mqtt contains] (bus-remote) -- (remote.west);
\draw[mqtt contains] (bus-http) -- (http.west);
\draw[mqtt contains] (bus-session) -- (session.west);
\draw[mqtt contains] (bus-sub) -- (sub.west);
\draw[mqtt contains] (bus-pub) -- (pub.west);

\node[mqtt note,minimum width=78mm,text width=72mm,below=\MQGapM of instance,xshift=-10mm] (note)
  {All five are sibling sections owned by the selected connection instance. \texttt{sub} and \texttt{pub} may share one connection; WebSocket instances additionally use \texttt{http}. Runtime use/order is not the configuration hierarchy.};

\begin{pgfonlayer}{background}
  \node[mqtt figure frame,fit=(root)(instance)(remote)(http)(session)(sub)(pub)(note)] (frame) {};
\end{pgfonlayer}
\node[mqtt section title] at ($(frame.north west)+(\MQTitleGap,-\MQTitleGap)$)
  {MQTTCli connection-instance configuration};

\end{tikzpicture}
\end{document}
