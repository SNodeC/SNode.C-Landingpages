\documentclass[tikz,border=8pt]{standalone}
\usepackage[T1]{fontenc}
\usepackage{lmodern}
\input{mqttsystem-figure-system.tex}
\begin{document}
\begin{tikzpicture}[mqtt mobile]
\tikzset{
  main/.style={minimum width=58mm,text width=51mm},
  side/.style={minimum width=28mm,text width=23mm}
}

\node[mqtt external,main] (brokerin) {Broker delivers matching PUBLISH};
\node[mqtt application,main,below=\MQGapS of brokerin] (client)
  {MQTTIntegrator MQTT client connection};
\node[mqtt control,main,below=\MQGapS of client] (tree)
  {topic-tree match — literal / \texttt{+} / terminal \texttt{\#}};
\node[mqtt control,main,below=\MQGapM of tree] (rules)
  {static / value / JSON mapping rules};
\node[mqtt process,main,below=\MQGapS of rules] (outputs)
  {zero / one / many immediate or scheduled outputs\\with their own QoS + retain};
\node[mqtt process,main,below=\MQGapS of outputs] (brokerout)
  {mapped PUBLISH returns through the same MQTT client connection};

\draw[mqtt data flow] (brokerin.south) -- (client.north);
\draw[mqtt data flow] (client.south) -- (tree.north);
\draw[mqtt control flow] (tree.south) -- (rules.north);
\draw[mqtt control flow] (rules.south) -- (outputs.north);
\draw[mqtt data flow] (outputs.south) -- (brokerout.north);
\draw[mqtt data flow] (brokerout.west) -- ++(-8mm,0) |- ($(client.west)+(-8mm,0)$) -- (client.west);

\node[mqtt note,side,right=8mm of tree] (subqos)
  {SUBSCRIBE uses the mapping's subscription QoS};
\draw[mqtt association] (tree.east) -- (subqos.west);
\node[mqtt note,side,right=8mm of outputs] (pubqos)
  {output QoS controls outgoing PUBLISH};
\draw[mqtt association] (outputs.east) -- (pubqos.west);

\begin{pgfonlayer}{background}
  \node[mqtt figure frame,fit=(brokerin)(client)(tree)(rules)(outputs)(brokerout)(subqos)(pubqos)] (frame) {};
  \node[mqtt data plane,fit=(brokerin)(client)(tree)(rules)(outputs)(brokerout)] (flow) {};
\end{pgfonlayer}
\node[mqtt section title] at ($(frame.north west)+(\MQTitleGap,-\MQTitleGap)$)
  {Integrator pipeline};
\node[mqtt annotation,anchor=east] at ($(frame.north east)+(-\MQTitleGap,-\MQTitleGap)$)
  {subscribe QoS $\neq$ publish QoS};

\end{tikzpicture}
\end{document}
