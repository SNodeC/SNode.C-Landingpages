\documentclass[tikz,border=0pt]{standalone}
\usepackage[T1]{fontenc}
\usepackage{lmodern}
\input{mqttsystem-figure-system.tex}
\input{snodec-canonical-figure-system.tex}
\begin{document}
\begin{tikzpicture}[mqtt desktop]
\tikzset{
  stage/.style={mqtt config,minimum width=68mm,text width=62mm,minimum height=16mm},
  field/.style={mqtt process,minimum width=60mm,text width=54mm,minimum height=21mm},
  fieldgroup/.style={mqtt group,inner sep=6mm}
}
\def\MQHierarchySpan{8.3mm}
\def\MQGroupLead{14.3mm}

\node[stage] (root)
  {\textbf{definition root}\\required: \texttt{bridges[]}};
\node[stage,below=\MQHierarchySpan of root] (bridge)
  {\textbf{logical bridge}\\required: \texttt{name}, \texttt{brokers[]}\\peer fields: \texttt{disabled}, \texttt{prefix}};
\node[stage,below=\MQHierarchySpan of bridge] (member)
  {\textbf{broker member}\\one \texttt{brokers[]} entry\\required: \texttt{network}};
\draw[mqtt contains] (root.south) -- (bridge.north);
\draw[mqtt contains] (bridge.south) -- (member.north);

\node[field,below=\MQGroupLead of member,xshift=-34mm] (network)
  {\texttt{network}\\\texttt{instance\_name}\\protocol + address\\transport + encryption};
\node[field,right=\MQHorizontalGap of network] (mqtt)
  {\texttt{mqtt} session\\client id + keep alive\\clean session + will\\credentials + loop prevention};
\node[field,below=\MQHorizontalGap of network] (topics)
  {\texttt{topics[]}\\topic filter\\subscription QoS};
\node[field,right=\MQHorizontalGap of topics] (scalars)
  {\textbf{scalar fields}\\\texttt{disabled}\\\texttt{prefix}\\\texttt{session\_store}};

\node[fit=(network)(mqtt)(topics)(scalars),inner sep=6mm] (fieldBounds) {};
\draw[mqtt contains] (member.south) -- (fieldBounds.north);

\coordinate (titleY) at ($(root.north)+(0,\MQTitleGap)$);
\node[mqtt section title,anchor=south west] (figureTitle)
  at (fieldBounds.west |- titleY) {Bridge-definition hierarchy};

\begin{pgfonlayer}{background}
  \node[mqtt figure frame,fit=(figureTitle)(root)(bridge)(member)(fieldBounds)] (frame) {};
  \node[fieldgroup,fit=(network)(mqtt)(topics)(scalars)] (fields) {};
\end{pgfonlayer}
\node[mqtt annotation,anchor=north west]
  at ($(fields.north west)+(3mm,-2mm)$) {broker-member peer fields};

\end{tikzpicture}
\end{document}
