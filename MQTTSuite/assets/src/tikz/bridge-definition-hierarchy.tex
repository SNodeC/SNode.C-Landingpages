\documentclass[tikz,border=8pt]{standalone}
\usepackage[T1]{fontenc}
\usepackage{lmodern}
\input{mqttsystem-figure-system.tex}
\begin{document}
\begin{tikzpicture}[mqtt desktop]
\tikzset{nodebox/.style={mqtt process,minimum width=43mm,text width=39mm}}

\node[mqtt config,nodebox] (root) {definition root\\\texttt{bridges[]}};
\node[mqtt config,nodebox,right=\MQHorizontalGapLoose of root] (bridge) {logical bridge\\\texttt{name} required\\\texttt{disabled}, \texttt{prefix}\\\texttt{brokers[]}};
\draw[mqtt flow] (root.east) -- (bridge.west);

\node[mqtt config,nodebox,right=\MQHorizontalGapLoose of bridge] (member) {broker member\\\texttt{network} required\\\texttt{disabled}, \texttt{prefix}\\\texttt{session\_store}, \texttt{topics[]}};
\draw[mqtt flow] (bridge.east) -- (member.west);

\node[mqtt process,minimum width=43mm,text width=39mm,right=\MQHorizontalGapLoose of member,yshift=30mm] (network)
  {\texttt{network}\\instance name\\protocol + address\\transport + encryption};
\node[mqtt process,minimum width=43mm,text width=39mm,right=\MQHorizontalGapLoose of member] (mqtt)
  {\texttt{mqtt} session\\client id / keep alive\\clean session / will\\username / password\\loop prevention};
\node[mqtt process,minimum width=43mm,text width=39mm,right=\MQHorizontalGapLoose of member,yshift=-30mm] (topics)
  {\texttt{topics[]}\\topic filter +\\subscription QoS};
\draw[mqtt flow] (member.east) -- (network.west);
\draw[mqtt flow] (member.east) -- (mqtt.west);
\draw[mqtt flow] (member.east) -- (topics.west);

\node[mqtt note,minimum width=63mm,text width=58mm,below=\MQGapM of bridge,xshift=45mm] (note)
  {Member prefixes and session-store paths sit beside network/MQTT/topic sections. Prefix separators are literal configuration, not inserted automatically.};

\begin{pgfonlayer}{background}
  \node[mqtt lane,fit=(root)(bridge)(member)(network)(mqtt)(topics)(note)] (frame) {};
\end{pgfonlayer}
\node[mqtt section title] at ($(frame.north west)+(\MQTitleGap,-\MQTitleGap)$) {Bridge-definition hierarchy};
\end{tikzpicture}
\end{document}
