\documentclass[tikz,border=8pt]{standalone}
\usepackage[T1]{fontenc}
\usepackage{lmodern}
\input{mqttsystem-figure-system.tex}
\input{snodec-canonical-figure-system.tex}
\begin{document}
\begin{tikzpicture}[mqtt desktop]
\tikzset{
  broker/.style={mqtt external,minimum width=31mm,text width=26mm},
  member/.style={mqtt application,minimum width=38mm,text width=32mm},
  core/.style={mqtt control,minimum width=42mm,text width=36mm},
  peer/.style={mqtt application,minimum width=32mm,text width=27mm},
  peerbroker/.style={mqtt external,minimum width=32mm,text width=27mm}
}

\node[broker] (a) {Broker A\\source domain};
\node[member,right=\MQHorizontalGap of a] (ma)
  {member A\\configured subscriptions\\select the input};
\node[core,right=\MQHorizontalGap of ma] (bridge)
  {logical bridge\\fan-out forwarding};

\draw[mqtt data flow] (a.east) -- node[mqtt topic label,above]{selected PUBLISH} (ma.west);
\draw[mqtt data flow] (ma.east) -- (bridge.west);

\node[mqtt state,below=\MQGapM of bridge,minimum width=74mm,text width=68mm] (fanout)
  {destination set = currently connected bridge members\\except the origin member A};
\draw[mqtt data flow] (bridge.south) -- (fanout.north);

\node[peer,below=\MQGapM of fanout,xshift=-21mm] (mb) {member B\\destination};
\node[peer,below=\MQGapM of fanout,xshift=21mm] (mc) {member C\\destination};
\node[peerbroker,below=\MQGapS of mb] (b) {Broker B};
\node[peerbroker,below=\MQGapS of mc] (c) {Broker C};

\draw[mqtt data flow] \MQPortSouth{fanout}{.30} -- ++(0,-6mm) -| (mb.north);
\draw[mqtt data flow] \MQPortSouth{fanout}{.70} -- ++(0,-6mm) -| (mc.north);
\draw[mqtt data flow] (mb.south) -- (b.north);
\draw[mqtt data flow] (mc.south) -- (c.north);

\node[mqtt note,below=\MQGapM of b,xshift=21mm,minimum width=94mm,text width=88mm] (preserve)
  {payload / message bytes, received QoS, and retain are preserved;\\the forwarded topic is rebuilt from bridge/member prefixes — see Figure 14};

\coordinate (titleguard) at ($(a.north)+(0,\MQGapL)$);
\begin{pgfonlayer}{background}
  \node[mqtt figure frame,fit=(titleguard)(a)(ma)(bridge)(fanout)(mb)(mc)(b)(c)(preserve)] (frame) {};
\end{pgfonlayer}
\node[mqtt section title] at ($(frame.north west)+(\MQTitleGap,-\MQTitleGap)$)
  {Logical bridge forwarding};

\end{tikzpicture}
\end{document}
