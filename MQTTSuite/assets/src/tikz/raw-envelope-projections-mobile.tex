\documentclass[tikz,border=8pt]{standalone}
\usepackage[T1]{fontenc}
\usepackage{lmodern}
\input{mqttsystem-figure-system.tex}
\begin{document}
\begin{tikzpicture}[mqtt mobile]
\tikzset{
  wide/.style={minimum width=72mm,text width=64mm},
  half/.style={minimum width=32mm,text width=27mm}
}

\node[mqtt external,wide] (publish) {received MQTT PUBLISH};
\node[mqtt control,wide,below=\MQGapS of publish] (store) {MQTTStore receive / store path};
\draw[mqtt data flow] (publish.south) -- (store.north);

\node[mqtt database,half,below=\MQBranchGap of store,xshift=-20mm] (raw)
  {raw INSERT attempt\\for every PUBLISH};
\node[mqtt decision,below=\MQBranchGap of store,xshift=20mm] (eligible)
  {valid JSON\\+ projection match?};
\draw[mqtt data flow] \MQPortSouth{store}{.30} -- ++(0,-6mm) -| node[mqtt edge label,near end,left]{1} (raw.north);
\draw[mqtt control flow] \MQPortSouth{store}{.70} -- ++(0,-6mm) -| node[mqtt edge label,near end,right]{2} (eligible.north);

\node[mqtt note,half,below=\MQGapS of raw] (rawnote)
  {raw success/failure does not gate projections};
\draw[mqtt association] (raw.south) -- (rawnote.north);

\node[mqtt process,half,below=\MQGapM of eligible,xshift=-20mm] (projection)
  {extract topic / JSON / literal values};
\node[mqtt note,half,below=\MQGapM of eligible,xshift=20mm] (skip)
  {NO: projection skipped};
\draw[mqtt control flow] \MQPortSouth{eligible}{.32} -- ++(0,-6mm) -| node[mqtt branch label,near end,left]{YES} (projection.north);
\draw[mqtt association] \MQPortSouth{eligible}{.68} -- ++(0,-6mm) -| node[mqtt branch label,near end,right]{NO} (skip.north);

\node[mqtt database,half,below=\MQGapS of projection] (typed)
  {typed INSERT attempt(s)\\zero / one / many};
\draw[mqtt data flow] (projection.south) -- (typed.north);

\node[mqtt note,wide,below=\MQGapM of typed,xshift=20mm] (note)
  {raw INSERT is issued first; projection INSERT outcomes remain independent};

\begin{pgfonlayer}{background}
  \node[mqtt figure frame,fit=(publish)(store)(raw)(eligible)(rawnote)(projection)(skip)(typed)(note)] (frame) {};
\end{pgfonlayer}
\node[mqtt section title] at ($(frame.north west)+(\MQTitleGap,-\MQTitleGap)$)
  {Raw envelope first, optional projections};

\end{tikzpicture}
\end{document}
