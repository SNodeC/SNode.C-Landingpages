\documentclass[tikz,border=0pt]{standalone}
\usepackage[T1]{fontenc}
\usepackage{lmodern}
\input{mqttsystem-figure-system.tex}
\input{snodec-canonical-figure-system.tex}
\begin{document}
\begin{tikzpicture}[mqtt desktop]
\tikzset{
  parent/.style={mqtt control,minimum width=42mm,text width=36mm},
  scan/.style={mqtt control,minimum width=70mm,text width=64mm},
  candidate/.style={mqtt config,minimum width=38mm,text width=32mm,minimum height=16mm},
  example/.style={mqtt note,minimum width=38mm,text width=32mm,minimum height=24mm},
  warningwide/.style={mqtt warning,minimum width=104mm,text width=98mm},
  qosnote/.style={mqtt note,minimum width=82mm,text width=76mm}
}
\def\MQArrowSpan{8.3mm}

% Matching context and evaluation rule.
\node[parent] (root) {one parent topic level\\\texttt{devices}};
\node[scan,below=\MQArrowSpan of root] (scan)
  {test siblings 1 → 2 → 3 in document order\\stop at the first matching branch};
\draw[mqtt control flow] (root.south) -- (scan.north);

% Example sibling array. The middle candidate is centered below the matcher;
% equal-width siblings then extend symmetrically left/right with canonical gaps.
\node[candidate,below=\MQArrowSpan of scan] (plus)
  {2 · single-level sibling\\\texttt{+}};
\node[candidate,left=\MQArrowSpan of plus] (literal)
  {1 · literal sibling\\\texttt{room-01}};
\node[candidate,right=\MQArrowSpan of plus] (hash)
  {3 · terminal multi-level sibling\\\texttt{\#}};

\node[example,below=\MQGapM of literal] (elit)
  {\texttt{devices/room-01}\\literal matches the exact level};
\node[example,below=\MQGapM of plus] (eplus)
  {\texttt{devices/lab}\\\texttt{+} consumes exactly one level};
\node[example,below=\MQGapM of hash] (ehash)
  {\texttt{devices/lab/x}: \texttt{\#} consumes the remainder.\\\texttt{devices}: child \texttt{\#} only if the matched parent has no subscription.};

% Enter the first sibling candidate; later candidates are tried only after no match.
\coordinate (scanLiteralMid) at ($(scan.south)!0.5!(literal.north)$);
\draw[mqtt control flow]
  (scan.south) -- (scan.south |- scanLiteralMid)
  -- (literal.north |- scanLiteralMid) -- (literal.north);
\draw[mqtt control flow] (literal.east) -- (plus.west);
\draw[mqtt control flow] (plus.east) -- (hash.west);

% Examples explain each candidate; they are associations, not runtime steps.
\draw[mqtt association] (literal.south) -- (elit.north);
\draw[mqtt association] (plus.south) -- (eplus.north);
\draw[mqtt association] (hash.south) -- (ehash.north);

% Ordering and QoS qualifications are explanatory, not process stages.
\node[warningwide,below=\MQGapM of eplus] (order)
  {1 → 2 → 3 is only this example's document order. There is no built-in literal → \texttt{+} → \texttt{\#} precedence.};
\node[qosnote,below=\MQGapM of order] (qos)
  {Subscription QoS belongs to the selected topic branch.};

% Relative title and neutral frame geometry.
\coordinate (titleY) at ($(root.north)+(0,\MQTitleGap)$);
\node[mqtt section title,anchor=south west] (figureTitle)
  at (literal.west |- titleY) {Topic-tree matching};
\begin{pgfonlayer}{background}
  \node[mqtt figure frame,fit=(figureTitle)(root)(scan)(literal)(plus)(hash)(elit)(eplus)(ehash)(order)(qos)] (frame) {};
\end{pgfonlayer}
\end{tikzpicture}
\end{document}
