\documentclass[tikz,border=8pt]{standalone}
\usepackage[T1]{fontenc}
\usepackage{lmodern}
\input{mqttsystem-figure-system.tex}
\begin{document}
\begin{tikzpicture}[mqtt mobile]
\tikzset{
  wide/.style={minimum width=72mm,text width=64mm},
  half/.style={minimum width=32mm,text width=27mm}
}

\node[mqtt external,wide] (incoming) {incoming MQTT PUBLISH};
\node[mqtt application,wide,below=\MQGapS of incoming] (broker)
  {Broker publish-processing path};
\draw[mqtt data flow] (incoming.south) -- (broker.north);

\node[mqtt process,half,below=\MQBranchGap of broker,xshift=-20mm] (normal)
  {ordinary routing\\to subscribers};
\node[mqtt control,half,below=\MQBranchGap of broker,xshift=20mm] (mapper)
  {optional shared\\\texttt{MqttMapper}};
\draw[mqtt data flow] \MQPortSouth{broker}{.30} -- ++(0,-6mm) -| (normal.north);
\draw[mqtt control flow] \MQPortSouth{broker}{.70} -- ++(0,-6mm) -| (mapper.north);

\node[mqtt process,half,below=\MQGapS of mapper] (mapped)
  {zero / one / many\\mapped PUBLISH messages};
\draw[mqtt control flow] (mapper.south) -- (mapped.north);
\draw[mqtt data flow] (mapped.east) -- ++(8mm,0) |- ($(broker.east)+(8mm,0)$) -- (broker.east);

\node[mqtt note,wide,below=\MQGapM of mapped,xshift=-20mm] (note)
  {Mapped outputs re-enter the same publish-processing path and may be mapped again. No child \texttt{mqttintegrator} process.};

\begin{pgfonlayer}{background}
  \node[mqtt figure frame,fit=(incoming)(broker)(normal)(mapper)(mapped)(note)] (frame) {};
  \node[mqtt data plane,fit=(incoming)(broker)(normal)] (data) {};
  \node[mqtt group,fit=(mapper)(mapped)(note)] (mapping) {};
\end{pgfonlayer}
\node[mqtt section title] at ($(frame.north west)+(\MQTitleGap,-\MQTitleGap)$)
  {Embedded mapper};

\end{tikzpicture}
\end{document}
