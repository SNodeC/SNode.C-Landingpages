\documentclass[tikz,border=0pt]{standalone}
\usepackage[T1]{fontenc}
\usepackage{lmodern}
\input{mqttsystem-figure-system.tex}
\input{snodec-canonical-figure-system.tex}
\begin{document}
\begin{tikzpicture}[mqtt mobile]
\tikzset{
  root/.style={minimum width=72mm,text width=64mm,minimum height=14mm},
  inst/.style={minimum width=72mm,text width=64mm,minimum height=14mm},
  peer/.style={mqtt config,minimum width=31mm,text width=26mm,minimum height=22mm},
  wideNote/.style={mqtt note,minimum width=72mm,text width=64mm,minimum height=16mm}
}
\def\OwnGap{8.3mm}

\node[mqtt application,root] (app)
  {\textbf{application root}\\e.g. \texttt{mqttcli}};
\node[mqtt control,inst,below=\OwnGap of app] (instance)
  {\textbf{named connection / listener instance}\\e.g. \texttt{in-mqtt}};
\draw[mqtt contains] (app.south) -- (instance.north);

\node[peer,below=10mm of instance,xshift=-18.5mm] (endpoint)
  {\textbf{endpoint + transport}\\\texttt{remote} / \texttt{local}\\address + socket / TLS};
\node[peer,right=6mm of endpoint] (web)
  {\textbf{HTTP / WebSocket}\\optional\\instance-family dependent};
\node[peer,below=6mm of endpoint] (session)
  {\textbf{MQTT session}\\CONNECT / session settings\\when MQTT is used};
\node[peer,right=6mm of session] (appspecific)
  {\textbf{app-specific}\\e.g. \texttt{sub}, \texttt{pub}\\or \texttt{db} with nested\\\texttt{storage}};
\node[fit=(instance)(endpoint)(web)(session)(appspecific),inner sep=4.5mm] (instanceBounds) {};

\node[wideNote,below=6mm of instanceBounds] (ownership)
  {Peer section families belong to the selected instance. Exact sets vary. This is configuration ownership, not runtime protocol layering or execution order.};
\node[wideNote,below=8mm of ownership] (values)
  {\textbf{value precedence}\\API / defaults $<$ configuration file $<$ command line};
\node[wideNote,below=6mm of values] (inspect)
  {\textbf{introspection}\\tree: \texttt{--help=expanded}\\state: \texttt{--show-config}\\\texttt{--command-line=...}};
\node[fit=(values)(inspect),inner sep=4.5mm] (surfaceBounds) {};

\coordinate (titleY) at ($(app.north)+(0,\MQTitleGap)$);
\node[mqtt section title,anchor=south west] (figureTitle)
  at ($(instanceBounds.west |- titleY)$) {Configuration ownership hierarchy};
\begin{pgfonlayer}{background}
  \node[mqtt figure frame,fit=(figureTitle)(app)(instanceBounds)(ownership)(surfaceBounds)] {};
  \node[mqtt group,fit=(instance)(endpoint)(web)(session)(appspecific),inner sep=4.5mm] (instanceGroup) {};
  \node[mqtt group,fit=(values)(inspect),inner sep=4.5mm] (surfaceGroup) {};
\end{pgfonlayer}
\node[mqtt edge label,anchor=west] at ($(instanceGroup.north west)+(3mm,0)$)
  {selected instance — peer sections};
\node[mqtt edge label,anchor=west] at ($(surfaceGroup.north west)+(3mm,0)$)
  {configuration surfaces};
\end{tikzpicture}
\end{document}
