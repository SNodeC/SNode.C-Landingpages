\documentclass[tikz,border=8pt]{standalone}
\usepackage[T1]{fontenc}
\usepackage{lmodern}
\input{mqttsystem-figure-system.tex}
\begin{document}
\begin{tikzpicture}[mqtt mobile]
\tikzset{
  wide/.style={minimum width=72mm,text width=64mm},
  half/.style={minimum width=34mm,text width=29mm}
}

\node[mqtt data,wide] (direct)
  {Direct MQTT listeners → shared Broker/session state};

\node[mqtt external,wide,below=\MQMobileLaneGap of direct] (clients)
  {browser / HTTP or MQTT-WebSocket client};
\node[mqtt control,wide,below=\MQMobileLaneGap of clients] (router)
  {shared HTTP / HTTPS listener + Express router};

\node[mqtt data,half,below=\MQGapM of router,xshift=-19mm] (ws)
  {MQTT WebSocket\\\texttt{/}, \texttt{/ws}, \texttt{/mqtt}};
\node[mqtt control,half,below=\MQGapM of router,xshift=19mm] (admin)
  {dashboard + JSON\\admin API + SSE};
\draw[mqtt data flow] \MQPortSouth{router}{.30} -- ++(0,-6mm) -| (ws.north);
\draw[mqtt control flow] \MQPortSouth{router}{.70} -- ++(0,-6mm) -| (admin.north);

\coordinate (surfacebottom) at ($(ws.south)!0.5!(admin.south)$);
\node[mqtt warning,wide,below=\MQGapM of surfacebottom] (risk)
  {No Broker application authentication on operator routes; wildcard API/event CORS; client events can include supplied MQTT password data.};
\node[mqtt note,wide,below=\MQGapS of risk] (note)
  {HTTPS is transport protection, not authorization. Protect the shared HTTP exposure with trusted network policy or path-aware external access control.};
\draw[mqtt association] (admin.south) -- ++(0,-5mm) -| \MQPortNorth{risk}{.70};
\draw[mqtt association] (risk.south) -- (note.north);

\begin{pgfonlayer}{background}
  \node[mqtt figure frame,fit=(direct)(clients)(router)(ws)(admin)(risk)(note)] (frame) {};
  \node[mqtt trust boundary,fit=(router)(ws)(admin)(risk)(note)] (trust) {};
\end{pgfonlayer}
\node[mqtt section title] at ($(frame.north west)+(\MQTitleGap,-\MQTitleGap)$)
  {MQTTBroker exposure boundary};
\node[mqtt annotation,anchor=south,text width=62mm,align=center,fill=MQSurface]
  at ($(trust.north)+(0,4mm)$)
  {required external / trusted deployment boundary — not enforced by MQTTBroker};

\end{tikzpicture}
\end{document}
