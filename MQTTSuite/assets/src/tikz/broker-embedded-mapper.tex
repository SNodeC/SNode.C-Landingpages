\documentclass[tikz,border=8pt]{standalone}
\usepackage[T1]{fontenc}
\usepackage{lmodern}
\input{mqttsystem-figure-system.tex}
\begin{document}
\begin{tikzpicture}[mqtt desktop]
\tikzset{stage/.style={mqtt process,minimum width=43mm,text width=39mm}}

\node[mqtt external,stage] (incoming) {incoming MQTT PUBLISH};
\node[mqtt application,stage,right=\MQHorizontalGapLoose of incoming] (broker) {Broker routing\\normal subscribers};
\node[mqtt success,stage,right=\MQHorizontalGapLoose of broker] (normal) {ordinary routed PUBLISH};
\draw[mqtt data flow] (incoming.east) -- (broker.west);
\draw[mqtt data flow] (broker.east) -- (normal.west);

\node[mqtt control,minimum width=47mm,text width=42mm,below=\MQGapL of broker] (mapper)
  {optional shared \texttt{MqttMapper}\\inside MQTTBroker};
\node[mqtt process,minimum width=50mm,text width=45mm,right=\MQHorizontalGapLoose of mapper] (mapped)
  {zero / one / many mapped PUBLISH messages\\immediate or scheduled};

\draw[mqtt control flow] (incoming.south) |- (mapper.west);
\draw[mqtt control flow] (mapper.east) -- (mapped.west);
\draw[mqtt data flow] (mapped.north) |- (broker.south east);

\node[mqtt note,minimum width=54mm,text width=49mm,below=\MQGapM of mapper] (note)
  {Mapped outputs re-enter the same Broker.\\No child \texttt{mqttintegrator} process.};
\draw[mqtt association] (mapper.south) -- (note.north);

\begin{pgfonlayer}{background}
  \node[mqtt data plane,fit=(incoming)(broker)(normal)] (data) {};
  \node[mqtt group,fit=(mapper)(mapped)(note)] (mapping) {};
  \node[mqtt lane,fit=(data)(mapping)] (frame) {};
\end{pgfonlayer}
\node[mqtt section title] at ($(frame.north west)+(\MQTitleGap,-\MQTitleGap)$) {Optional embedded mapping inside MQTTBroker};
\end{tikzpicture}
\end{document}
