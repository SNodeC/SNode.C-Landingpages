\documentclass[tikz,border=8pt]{standalone}
\usepackage[T1]{fontenc}
\usepackage{lmodern}
\input{mqttsystem-figure-system.tex}
\begin{document}
\begin{tikzpicture}[mqtt desktop]

\tikzset{
  role/.style={mqtt application,minimum width=54mm,text width=48mm,minimum height=20mm},
  brokerrole/.style={mqtt application,minimum width=58mm,text width=52mm,minimum height=48mm}
}

\node[brokerrole] (broker)
  {\textbf{MQTTBroker}\\[2mm]
   MQTT 3.1.1 broker + topic space\\
   shared session / subscription state};

\node[role,right=\MQHorizontalGapLoose of broker,yshift=17mm] (integrator)
  {\textbf{MQTTIntegrator}\\transform payloads/topics\\and republish};
\node[role,right=\MQHorizontalGap of integrator] (bridge)
  {\textbf{MQTTBridge}\\forward selected publications\\between broker domains};

\node[role,right=\MQHorizontalGapLoose of broker,yshift=-17mm] (cli)
  {\textbf{MQTTCli}\\publish, subscribe,\\and inspect traffic};
\node[role,right=\MQHorizontalGap of cli] (store)
  {\textbf{MQTTStore}\\persist raw MQTT envelopes\\and optional projections};

\node[mqtt note,below=\MQBranchGap of broker,anchor=north west,
      minimum width=126mm,text width=118mm] (note)
  {Five separate executables. MQTTIntegrator, MQTTBridge, MQTTCli, and MQTTStore
   act as MQTT clients where configured; compose only the roles a deployment needs.};

\begin{pgfonlayer}{background}
  \node[mqtt figure frame,fit=(broker)(integrator)(bridge)(cli)(store)(note)] (frame) {};
\end{pgfonlayer}
\node[mqtt section title] at ($(frame.north west)+(\MQTitleGap,-\MQTitleGap)$)
  {Five applications, five responsibilities};

\end{tikzpicture}
\end{document}
